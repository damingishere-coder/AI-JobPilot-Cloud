# P9 额度系统与基础后台

P9 在不接入真实支付的前提下，完成免费额度、额度流水、额度限制和最小运营后台。后端权限与数据库 RLS 是最终安全边界，前端角色守卫仅用于隐藏入口和避免页面闪现。

## 1. 本轮范围

- 免费用户每个 UTC 自然月拥有 20 次 AI 分析额度和 10 次投递确认额度。
- 预留月卡、高级月卡、求职季卡、人工陪跑版套餐代码；本轮只能由管理员人工设置套餐与额度。
- 不接支付供应商、不创建支付订单、不处理支付回调，也不自动续费。
- 普通用户可查询自己的额度；只有状态为 `ACTIVE` 且角色为 `ADMIN` 的用户可以访问后台接口。

## 2. 数据库

Flyway `V11__quota_admin_foundation.sql` 新增：

- `app.user_quotas`：按用户、资源和 UTC 自然月保存套餐、总量、已用、预占、周期与版本；数据库约束保证 `used_amount + reserved_amount <= limit_amount`。
- `app.quota_usage_logs`：只追加的额度流水，记录 `RESERVE`、`COMMIT`、`RELEASE`、`ADJUST`；稳定的 `operation_key` 唯一约束阻止重复扣减。
- `app.subscriptions`：仅预留套餐周期与状态结构，没有支付服务或支付凭证。
- `app.job_matches.quota_reservation_key`：把 AI 分析任务与其额度预占绑定，供 Worker 在终态结算。

已有未删除用户会回填当月免费额度；新用户在注册事务中初始化免费额度。上述表都启用 RLS，额度流水不允许普通应用路径更新或删除。

## 3. 扣减与返还规则

### AI 分析

1. 新的 AI 分析动作入队前预占 1 次 `AI_ANALYSIS` 额度。
2. 分析最终成功时把预占提交为已用；同一任务的队列重试不会再次预占。
3. 分析最终失败时释放预占，因此失败会返还额度；尚未到终态的重试、租约恢复不会提前结算。
4. 相同幂等请求或命中已有任务时返回原结果，不重复扣减。用户明确发起新的强制分析属于新动作，会重新检查并占用额度。

### 投递确认

1. 创建投递清单任务不扣额度；通过全部版本、状态和设备校验后，确认投递前消耗 1 次 `DELIVERY_CONFIRM` 额度。
2. 同一确认请求的幂等重放返回原结果，不重复扣减。
3. 插件执行成功或失败都不再改变该次额度。失败不返还，因为额度计量点是一次有效的用户确认与投递机会，而不是招聘平台最终结果。
4. 暂停或可重试失败后，用户再次明确确认属于新的投递机会，会再次消耗 1 次；系统仍禁止批量确认或绕过用户确认。

额度不足统一返回 HTTP 429、错误码 `QUOTA_EXCEEDED` 和明确的剩余额度提示。每次真实的预占、提交、释放、直接消耗或管理员调整都会写入 `quota_usage_logs`。

## 4. 接口

用户接口：

- `GET /api/quota/me`：仅返回当前用户套餐、周期以及 AI 分析/投递确认额度，不返回流水幂等键或其他用户数据。

管理员接口（全部要求 Web Session、`ACTIVE ADMIN`；写接口还要求 CSRF）：

- `GET /api/admin/users`
- `GET /api/admin/users/{id}`
- `GET /api/admin/users/{id}/quota`
- `PUT /api/admin/users/{id}/quota`，必须提供 `Idempotency-Key` 和调整原因
- `GET /api/admin/dashboard`
- `GET /api/admin/audit-logs`
- `GET /api/admin/delivery-failures`

管理员跨用户查询只经过固定签名、固定 `search_path` 的窄数据库函数；函数内部再次校验管理员身份。调额不能把总量设置到低于当前“已用 + 预占”，成功调整同时写额度流水与审计日志。

## 5. 后台页面与敏感数据

- `/admin`：系统统计、用户列表、用户额度、岗位/分析/任务/成功/失败/插件设备数量、最近失败任务和最近审计日志。
- `/admin/user?id={userId}`：用户详情、当前周期额度明细和人工调整表单。

普通用户侧边栏不渲染后台入口，访问后台路径会被前端守卫跳转；后端仍独立执行管理员权限校验。后台只接收后端已经脱敏的 `emailMasked`，当前用户表没有手机号字段；接口和页面不返回完整邮箱、密码哈希、Token、API Key、Cookie、简历正文、审计详情、IP 哈希或请求 ID。

## 6. 验证与后续边界

后端测试覆盖默认额度、扣减、失败返还、额度不足、幂等、防重复流水、RLS、普通用户拒绝、管理员访问和响应敏感字段检查。前端测试覆盖后台守卫、侧边栏入口、页面数据、脱敏展示和调额幂等键。

P9 不等于可以直接生产上线。下一轮应进入 P10，完成腾讯云上线前验收、备份恢复演练、安全配置检查、监控告警和灾难回滚流程。
