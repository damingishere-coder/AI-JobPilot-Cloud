# 第 4 轮：简历、求职目标与岗位池底座

本轮在第 3 轮用户系统之上增加用户私有求职资料，并继续坚持“Session 身份 + 服务层 user_id + PostgreSQL RLS”三层隔离。岗位池只交付安全的数据模型和浏览能力，真实插件采集与 AI 匹配没有提前开放。

## 已实现范围

### 简历

- 支持 PDF、DOCX、TXT，单文件最大 10 MiB；PDF 最多 50 页。
- 上传内容先经过 ClamAV，再校验扩展名、MIME、文件签名、DOCX 解压数量/体积和 TXT 编码。
- 原文件使用 AES-256-GCM 加密后写入私有卷或 S3，随机对象 Key 不含原文件名；提取文本也以密文写入 PostgreSQL。
- Worker 使用短租约异步提取文本，支持三次有界重试；前端仅显示只读预览。
- 上传与删除需要幂等键，删除使用 `If-Match` 乐观锁。删除后记录立即从用户查询中消失、提取文本立即清空，原文件由 Worker 最终物理清理。
- 上传、拒绝、解析成功/失败、删除申请和最终清理均写脱敏审计事件。

### 求职目标

- 支持目标职位、城市、薪资范围、经验、学历、行业、公司规模、优先/排除公司和排除关键词。
- 保存会创建递增的新版本，同一用户只有一个当前版本；旧版本保留用于后续解释 AI 结果。
- 客户端携带当前版本，冲突返回 `RESOURCE_VERSION_CONFLICT` 并重新加载服务端最新版。

### 岗位池

- 建立用户私有 `job_posts` 标准模型、平台/指纹去重约束和列表索引。
- 提供当前用户的筛选、分页、排序、列表和详情 API，以及对应 Web 页面。
- 本轮没有公开岗位写入 API；真实岗位采集留到插件绑定完成后的第 7 轮。AI 匹配、评分和队列仍属于第 5 轮。

## 用户隔离与 Worker 权限

`resumes`、`job_preferences`、`job_posts` 都直接保存 `user_id` 并启用 RLS。Controller 不接受业务 `user_id`，每个 Web 请求只使用登录 Session 中的用户身份。Worker 没有 Web Session，只能通过两个受限的 `SECURITY DEFINER` 函数领取一条解析或清理任务；真正更新时仍建立对应用户的事务级 RLS 上下文，并校验租约 Token。

集成测试覆盖 A 用户可见、B 用户不可见/不可更新、缺少事务用户上下文时查询为空，以及 Worker 只能通过窄函数领取任务。

## API 摘要

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/resumes/upload` | 安检、加密并异步解析简历 |
| `GET` | `/api/resumes/current` | 读取当前简历，可选择只读提取文本 |
| `GET` | `/api/resumes` | 读取当前用户上传历史 |
| `DELETE` | `/api/resumes/{id}` | 申请删除并触发最终物理清理 |
| `GET` | `/api/preferences` | 读取当前求职目标 |
| `PUT` | `/api/preferences` | 创建新的求职目标版本 |
| `GET` | `/api/jobs` | 筛选和分页读取岗位池 |
| `GET` | `/api/jobs/{id}` | 读取当前用户岗位详情 |

所有写接口都需要 Web Session 和 CSRF。插件 Token 不能读取或修改简历、偏好，也不能使用本轮 Web 岗位接口。

## 配置与运维

- 一键启动脚本会在 `.secrets/data_encryption_key` 生成 64 位十六进制 AES-256 Key。
- API 的 `/readyz` 同时检查 PostgreSQL、Redis、私有存储和 ClamAV；扫描服务不可用时上传失败关闭，不会绕过安检。
- ClamAV 病毒库保存在 `clamav-db` 卷，首次启动可能需要数分钟。
- 数据库、私有文件卷与加密 Key 必须作为一个恢复集合备份；Key 丢失时既有简历无法解密。
- 日志和审计详情不记录原文件、提取文本、完整岗位描述、密钥或对象内容。

## 明确未实现

- OCR、图片简历、在线简历编辑、AI 自动改写或公开下载链接。
- 插件绑定、岗位上传/导入、服务器端招聘平台爬取。
- AI 匹配、Redis Streams 消费、投递任务和额度扣减。
