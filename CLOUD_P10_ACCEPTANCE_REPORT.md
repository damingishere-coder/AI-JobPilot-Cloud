# AI-JobPilot-Cloud P10 腾讯云上线前验收记录

验收日期：2026-08-24

环境：腾讯云轻量应用服务器，OpenCloudOS 9.4，4 vCPU / 4 GiB / 40 GiB。

用途：仅预发布合成数据验证，不承载真实用户、真实简历、真实支付或真实招聘平台自动投递。

## 1. 当前结论

腾讯云预发布基础设施已经运行，但 P10 **尚未最终通过**，当前不能邀请真实用户。已部署服务器仍运行稳定的 `main@340c068`；Linux Secret、前端依赖安全和本轮日志/文档/日志轮转修复仍在 PR 分支，尚未部署。容器运行、内网端口隔离、数据库备份/独立恢复、Redis AOF、密钥登录等已完成；正式域名/备案/HTTPS、异地加密备份、账号级删除与合规确认、正式域名插件合成 E2E 仍是上线阻塞项。

本文件同时引用源码证据与 2026-08-24 对目标服务器执行的外部验收命令结果；它不是仅凭仓库静态检查得出的结论。`CLOUD_LAUNCH_CHECKLIST.md` 和 `CLOUD_PRIVACY_CHECKLIST.md` 保持为可复用的逐项模板，不因一次外部验收自动勾选；本文件记录本次环境的实际状态。

## 2. 已完成并有证据的项目

| 项目 | 结果 | 证据摘要 |
| --- | --- | --- |
| 服务器基础环境 | 通过 | Docker 29.3.1、Compose 2.32.1、Git 2.43.7、Nginx 1.29.8 已安装；2 GiB Swap 已启用 |
| 非 root 部署账户 | 通过 | 项目归属 `deploy`，该账户只通过 SSH 密钥登录并具备 Docker 组权限 |
| SSH 暴力破解收口 | 通过 | 密码登录关闭、root 远程登录关闭、公钥登录开启；腾讯云 22 端口只允许当前管理公网 IP |
| Compose 服务 | 通过 | PostgreSQL、Redis、ClamAV、Web、API、AI Worker、内层 Nginx 均为 healthy；迁移容器退出码 0 |
| 健康端点 | 通过 | 服务器回环访问 `/livez`、`/readyz`、`/api/health` 均返回 HTTP 200 |
| 公网端口隔离 | 通过 | 宿主机仅监听 22 和 `127.0.0.1:8080`；5432、6379、6866、8888、8889 未发布；Compose data 网络为 internal |
| 应用数据库角色 | 通过 | `jobpilot_app` 为 `super=false`、`bypassrls=false`；直接读取受限审计表被拒绝 |
| Secret 文件边界 | 通过 | 真实值只在服务器 `.secrets/`；目录与文件权限已限制；未打印、未提交真实内容 |
| 日志抽样 | 通过 | 最近运行日志按 Authorization、Bearer 值、密码值、Cookie 值、API Key 值、简历全文模式扫描均为 0 |
| PostgreSQL 备份 | 通过 | `p10-initial-20260824.dump` 已生成，SHA-256 复核通过 |
| PostgreSQL 独立恢复 | 通过 | 已恢复到独立临时数据库；11 个迁移、16 张 `app` 业务表、16 条 RLS 策略存在；应用角色越权读取审计表失败；临时库随后清理 |
| 上传文件备份格式 | 通过 | `p10-private-storage-20260824.tar.gz` SHA-256 与 tar 完整性复核通过；只包含预发布空/合成存储，不含真实简历 |
| Redis 持久化 | 通过 | AOF 已开启；Redis 仅承载 Session、限流、绑定码和队列缓冲，不作为业务事实源 |
| 服务器容量 | 通过（预发布） | 根盘约 31% 已用；容器全启时仍有约 1 GiB 可用内存，Swap 只少量使用 |

说明：Compose 日志轮转已在本轮分支加入，但服务器仍运行旧 `main`，必须在相关 PR 合并并重新创建容器后，才能把“服务器日志轮转已生效”判为通过。

## 3. 尚未完成的阻塞项

| 阻塞项 | 为什么必须完成 | 下一步责任边界 |
| --- | --- | --- |
| 正式域名与 ICP 备案 | 中国大陆公网服务不能把临时 IP 当正式入口；证书也需要稳定域名 | 用户决定/购买域名并完成实名认证与备案，Codex 可继续做 DNS、Nginx 和验证 |
| HTTPS 证书与外层 Nginx | `AUTH_COOKIE_SECURE=true` 下必须 HTTPS 才能正常登录，HTTP 不能承载正式 Session | 域名可用后由 Codex配置 80→443、证书、反代和续期检查 |
| 异地加密备份 | 当前备份与服务器同盘，主机/磁盘故障会同时丢失应用和备份 | 用户批准腾讯云 COS/快照等可能付费资源后，由 Codex配置加密上传、保留期和失败告警 |
| 上传文件真实恢复演练 | 当前归档格式已验证，但没有真实/合成非空加密简历对象可做解密恢复 | 仅使用合成简历完成一次非空上传、备份、恢复、解密和删除演练 |
| 账号级删除 | 当前只实现简历删除/清除，尚无完整账号自助删除 | 需要单独业务需求、实现、隐私政策和预发布演练；本次按要求不新增业务功能 |
| 隐私政策/用户协议/第三方 AI 说明 | 技术测试不能替代法律与用户告知 | 用户/法务定稿，系统上线时锁定版本并记录同意 |
| 正式域名插件 E2E | 静态测试不能证明浏览器扩展、HTTPS、Token、任务状态和平台页面真实链路一致 | 域名 HTTPS 完成后，只用合成账号/岗位逐条确认测试；保持执行开关关闭到验收通过 |
| PR 与部署版本统一 | 服务器仍运行已合并的稳定 main；Linux Secret、前端依赖安全和本轮补漏 PR 尚未合并部署 | CI 全绿后由用户明确批准合并，Codex再按顺序合并、部署并回归 |

## 4. Redis 持久化结论

Redis 需要开启 AOF，以减少重启导致的 Session、限流状态、绑定码和队列缓冲丢失；当前已开启。但 PostgreSQL 和私有上传文件才是业务事实源，恢复时应先恢复数据库和文件，Redis 可以清空重建。任何备份方案都不能用 Redis AOF 替代 PostgreSQL dump 和上传文件备份。

## 5. 用户数据删除结论

当前只具备单份简历的删除与异步文件清除；完整账号删除仍是设计状态。真实上线前至少需要：重新认证与二次确认、撤销 Session/插件 Token/绑定码、暂停未完成任务、按外键顺序删除或匿名化业务数据、保留最小审计、记录删除墓碑，并确保恢复旧备份后重新执行删除。该能力未实现前，不得宣称支持完整账号擦除，也不得邀请真实用户。

## 6. 当前允许与禁止

- 允许：继续运行预发布服务；使用合成账号、合成岗位、合成简历做验收；继续完善部署与安全配置。
- 禁止：导入真实用户或真实简历；接真实支付；保存/上传招聘平台 Cookie 或账号密码；绕过验证码/登录/风控；开启无确认自动投递；把临时公网 IP 当正式网址。

## 7. P10 最终通过条件

以下项目全部完成并留有证据后，才可把结论改为“小范围真实用户测试候选”：

1. 所有上线相关 PR 合并到 `main`，服务器部署同一 commit，完整回归通过。
2. 正式域名、ICP备案、DNS、可信 HTTPS、自动续期与 80→443 跳转通过。
3. 异地加密备份和非空上传文件恢复演练通过，记录实际 RPO/RTO。
4. 账号删除能力或经合规确认的人工受理方案落地并演练。
5. 隐私政策、用户协议和第三方 AI 数据说明定稿。
6. 正式域名下使用合成数据完成注册、简历、目标、岗位、AI、投递清单、插件绑定、逐条确认、结果回传和暂停闭环。
7. 最终安全扫描确认无凭证泄露、跨用户访问、后台越权或内部端口公网暴露。
