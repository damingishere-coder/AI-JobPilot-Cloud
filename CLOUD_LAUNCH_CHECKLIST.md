# AI-JobPilot-Cloud P10 上线前检查清单

本清单把“代码/配置静态检查”和“必须在腾讯云人工验证”的事项分开。当前仓库只完成代码与文档准备，**没有真实腾讯云部署、证书签发、备份恢复演练或真实用户准入结论**。

## 1. 判定规则

- `通过`：有命令退出码、配置输出、测试报告或人工截图/工单证据。
- `待人工验证`：代码和文档已具备，但尚未在腾讯云目标环境执行。
- `阻塞`：涉及公网、真实用户、隐私/法律、Secret、恢复或权限的关键项未通过。
- 只有所有阻塞项消除，且小范围合成账号验证通过，才可考虑受控真实用户测试；本清单不能替代隐私政策、用户协议或法律意见。

## 2. 代码与配置静态检查（可在仓库执行）

- [ ] `./gradlew test` 退出码为 `0`。
- [ ] `./gradlew build` 退出码为 `0`。
- [ ] `cd front && pnpm lint` 退出码为 `0`。
- [ ] `cd front && pnpm build` 退出码为 `0`。
- [ ] `docker compose config` 退出码为 `0`；输出确认只有 Nginx 绑定 `127.0.0.1:8080`。
- [ ] `nginx -t` 或现有 Nginx 容器内语法检查通过；工具缺失时记录未执行原因。
- [ ] 仓库卫生检查确认没有 `.env`、`.secrets/`、数据库、日志、备份、真实简历、Cookie、Token 或 API Key 被 Git 跟踪。
- [ ] Compose 的 `data` 网络仍为 internal；PostgreSQL、Redis、API、Worker、Web、ClamAV 没有宿主机端口映射。
- [ ] 宿主机 HTTPS 示例只反代 `127.0.0.1:8080`，80 跳转 443，证书和域名仍是部署时替换的占位值。
- [ ] 外层和 Compose 内层 Nginx 均对旧本地版页面及 `/api/cookie/**` 等本地凭证接口返回 404；Cloud 公网入口不会下载或调用 Cookie 保存功能。
- [ ] `.env.example` 的密码、Pepper、数据 Key、AI Key、云凭证均为空；非敏感 AI URL/模型与代码消费关系一致。
- [ ] 静态审阅确认项目使用 Redis Session 而非 JWT，插件 Token 是随机高熵值 + SHA-256，管理员没有环境变量自动提权路径。

## 3. 腾讯云资源与网络（必须人工验证）

- [ ] CVM、磁盘、Docker、Compose、时区和备份对象存储已按批准规格创建；记录资源 ID 和责任人。
- [ ] 安全组公网只开放 TCP 80/443；TCP 22 仅固定管理 IP/CIDR。
- [ ] 5432、6379、8888、8889、6866、8080 在安全组、本机防火墙、Docker 发布和 IPv6 规则中均不可从公网访问。
- [ ] DNS 解析指向实际入口；没有使用示例域名或临时公网 IP 作为正式地址。
- [ ] 证书由可信 CA 签发，私钥权限正确，续期和到期告警可收到；HTTP 到 HTTPS 跳转可验证。
- [ ] `AUTH_COOKIE_SECURE=true` 已设置；HTTPS 下注册、登录、记住我、刷新、退出和 CSRF 均通过，HTTP 不接受正式 Session。
- [ ] 外层 Nginx 到 `127.0.0.1:8080`、内层 Nginx 到 API/Web 的转发头、客户端 IP 和协议在真实链路中验证。

## 4. Secret、权限与日志（必须人工验证）

- [ ] `db_owner_password`、`db_app_password`、`redis_password`、`auth_hash_pepper`、`data_encryption_key`、`ai_api_key` 由 Secret/KMS 注入，未写入镜像、`.env`、Compose 或日志。
- [ ] 数据加密 Key 的备份、Key ID、轮换窗口和恢复责任人已登记；不能只备份数据库而不备份 Key。
- [ ] 运行角色 `jobpilot_app` 与迁移 owner 分离；普通用户不能访问 `/api/admin/**`，ADMIN 角色由人工受控流程授予。
- [ ] API/Worker 生产日志走 stdout/stderr，Docker 日志轮转已配置；抽样日志不含密码、Token、Authorization、Cookie、简历全文、岗位全文、Prompt 或 AI Key。
- [ ] Nginx 访问日志不含 Authorization/Cookie；监控、告警和 APM 采集范围已审阅。
- [ ] ClamAV 健康检查通过；上传大小、类型、恶意文件拒绝和私有存储不可公开访问已验证。

## 5. 部署、迁移与回滚（必须人工验证）

- [ ] 使用固定镜像 tag 和已审阅的 Compose/环境版本；记录代码 commit、镜像 digest 和 Flyway 版本。
- [ ] `migrate` 退出码为 `0`，API、Worker、Web、Nginx、PostgreSQL、Redis、ClamAV 全部健康。
- [ ] `/livez`、`/readyz`、`/api/health` 返回预期状态；Actuator 非公开路径不会从公网访问。
- [ ] 记录一次从旧镜像回滚到已验证版本的演练结果；数据库不可逆迁移已有单独回滚/恢复方案。
- [ ] 维护窗口、值班人、停用插件执行方式和事件响应联系人已确认。

## 6. 备份与恢复（必须人工验证）

- [ ] PostgreSQL 定时备份成功，备份已加密上传独立介质，失败/容量/KMS 错误会告警。
- [ ] `private-storage` 卷或 S3 Bucket 已开启版本化/快照/复制策略，并与数据库时间窗口对应。
- [ ] Redis AOF `appendonly yes` + `appendfsync everysec` 已核对；同时确认 Redis 不是事实源，不能替代数据库和上传文件备份。
- [ ] 使用 `ai_jobpilot_restore_*` 在独立验证库完成 `pg_restore`，记录退出码和耗时；没有实际演练不得勾选通过。
- [ ] 恢复后 RLS、应用角色权限、简历解密、岗位/匹配/任务读取和合成账号登录均通过。
- [ ] RPO/RTO 实测值、失败项、改进项和下一次演练日期已记录；备份保留期与隐私删除策略一致。

## 7. 隐私、删除与小范围准入（必须人工/合规验证）

- [ ] 隐私政策、服务条款、数据处理/第三方 AI 说明和保存期限已定稿，并在注册同意流程中使用正确版本。
- [ ] 已实现的简历删除/清除流程在预发布完成演练；原文件、提取文本、缓存和对象版本的清除证据齐全。
- [ ] 账号级自助彻底删除仍未实现时，已在产品与隐私政策中明确披露；该项未解决前不得把服务宣传为完整 GDPR 擦除能力。
- [ ] 已定义岗位、匹配、任务、额度、插件设备/Token、审计和备份的删除/匿名化顺序；普通备份建议 30 天自然过期，审计最小安全字段建议最多保留 180 天，最终期限须由法务/隐私政策确认。
- [ ] 使用合成账号 A/B 验证注册 → 上传测试简历 → 求职目标 → 岗位采集 → AI 匹配 → 投递清单 → 插件绑定/执行回传；不导入真实用户数据、不上传真实简历、不接真实支付。
- [ ] 插件只使用受限 Token，不上传招聘平台 Cookie、LocalStorage、SessionStorage、账号密码或完整页面；验证码、登录失效和风控均暂停并要求人工处理。
- [ ] 真实用户测试范围、人数、平台、时间、退出方式和客服/事件响应人已书面批准。

## 8. 最终闸门

以下任一项未完成，结论必须是“不可邀请真实用户”：

- 未实际部署到腾讯云并验证 HTTPS、Cookie、安全组和转发头；
- 未实际完成独立恢复库演练，或上传文件/数据 Key 无法恢复；
- 隐私政策、用户协议或 AI 第三方处理说明未定稿；
- 账号级删除未实现，或人工受理/删除方案尚未完成合规确认与预发布演练；
- 发现跨用户访问、普通用户可进后台、日志泄露凭证、公网暴露内部端口或插件保存/上传 Cookie；
- 真实用户准入仅凭代码测试通过，没有人工验收证据。

当前仓库的默认结论：**仅允许本地/预发布合成数据验证；P10 腾讯云人工项未完成，不能判定可进入真实用户测试。**
