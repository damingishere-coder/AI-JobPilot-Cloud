# 📑 更新日志

---

* 2026-08-01
    1. 发布 **1.3 多平台采集与分析增强版**，后端、前端和 Chrome 扩展版本对齐为 `1.3.0`。
    2. 收拢并合并智联搜索参数与安全恢复、Boss 扫描断点与异常页保护、Boss 搜索 API 采集 POC 等分支。
    3. Boss/智联 Chrome Bridge 增加扫描会话隔离、失效断点清理、详情页跳转次数保护和失败诊断。
    4. Boss 搜索 API POC 限制为固定接口、单关键词、单城市和第一页，并在普通失败时降级到页面采集。
    5. Boss 分析页补充分数线设置、AI 分数筛选、批量确认操作和统计查询能力。
    6. 增加后端控制器、服务、SQL Provider 和 Chrome 扩展测试，统一 README、使用指南、开发指南、架构说明、文档索引和版本说明。

---

* 2026-06-09
    1. 发布 **1.2 MVP 全流程打通**，后端、前端和 Chrome 扩展版本对齐为 `1.2.0`。
    2. 合并投递工作台、配置完整度检查、档案隔离、AI 分析状态流转、待确认卡片和投递分析看板能力。
    3. Boss 和智联 Chrome Bridge 路线完成“扫描岗位 → AI 分析 → 待确认 → 用户确认后投递 → 状态回写 → 分析统计”的 MVP 闭环。
    4. 优化智联跨页采集、详情页扫描、投递按钮识别、停止状态和分析图表稳定性。
    5. 补齐关键 service 测试与数据库初始化逻辑，减少分析状态和档案数据互相串扰。
    6. 同步更新 README、使用指南、开发指南、架构说明、文档索引和项目检查报告。

---

* 2026-05-27
    1. 项目阶段命名为 **1.0 自动投简历脚本**，后端、前端和 Chrome 扩展版本对齐为 `1.0.0`。
    2. 新增 Chrome Bridge 扩展路线：Boss 和智联支持复用用户已登录 Chrome 标签页扫描岗位、提交 AI 分析、待确认后投递。
    3. Boss/智联新增待确认投递接口、批量确认接口和投递结果回写接口。
    4. 新增 `JobRunCoordinator`，避免多平台任务并发争用同一个自动化上下文，并支持 Chrome 扫描停止 runId。
    5. 新增 Docker 本地开发配置和环境变量模板，便于前后端联调。
    6. 整理 Git 提交边界：移除 `.pnpm-store` 暂存，补充 `.gitignore` 与 `.dockerignore`，避免依赖缓存、数据库、日志和构建产物入库。
    7. 新增 `doc/1.0自动投简历脚本.md`，同步更新 README、使用指南、开发指南、架构说明、API 接口和项目检查报告。

---

* 2025-11-11
    1. 🖼️ 添加 GUI 配置：新增图形化配置入口与说明。
    2. 🧰 前端栈：Node.js 20.19、pnpm 构建、Next.js、Tailwind CSS v4、shadcn/ui。
    3. ⚙️ 后端栈：JDK 21、Spring Boot 3.5.7、Gradle 9.2、SQLite 持久化。

---

* 2025-10-18 14:30:00
    1. ✨ **内置1494+城市代码和2137+行业代码**：重构城市与行业代码加载方式，从硬编码改为JSON文件动态加载，支持全国所有地区。
    2. 🗑️ **彻底移除chromedriver依赖**：从Git历史中完全删除chromedriver.exe，项目体积从200MB优化至49MB，减少75%空间占用。
    3. 🔧 **修复企业微信推送功能**：优化Bot推送配置，支持从resources目录读取.env文件，简化配置流程。
    4. 🧹 **代码优化与清理**：删除Bark推送冗余代码，移除硬编码的城市枚举，代码更简洁易维护。
    5. 📋 **行业代码待完善**：city-industry-code.json已包含完整行业数据，后续将支持动态加载（择日更新）。
    6. 🐛 **修复nul文件问题**：优化bat脚本，解决Windows下产生nul文件的问题。

---

* 2025-08-08 04:33:56
    1. Boss直聘逻辑修正，现已能完整运行所有流程。
    2. 图片简历不能发送，AI打招呼功能正常。
    3. 目前boss驱动已从Selenium改为playwright。
    4. 新增gui分支，该分支为本项目的gui版本，主要为群管理：【凯】提供。
    5. 砍掉了bark推送，用户地区码，手机端等杂七杂八不重要的功能，返璞归真。

---

* 2024-08-12 22:56:20
    1. 添加企业微信消息推送功能
* 2024-08-12 22:56:20
    1. 添加企业微信消息推送功能
* 2024-08-11 18:39:56
    1. 修复智联，猎聘等不能投递的问题。
    2. 添加定时投递功能
* 2024-06-06 17:41:20
    1. boss支持多城市投递。
* 2024-06-06 01:49:38
    1. boos：若公司名小于2个字或4个字母则不会添加进黑名单
    2. 添加linux系统支持。
* 2024-04-28 15:20:06
    1. boos：自动更新黑名单公司
* 2024-04-15 01:52:18
    1. 新增config.yaml,目前仅需修改配置文件即可，已全平台支持
    2. cookie有效期延长，保持至少一周（拉勾平台除外）【安慰剂】
