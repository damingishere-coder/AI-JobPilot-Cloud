# AI-JobPilot-Cloud 安全与隐私原则

本文是“投递牛马 SaaS 云端版”第一版安全基线。任何功能若需要突破这些边界，应停止开发并先进行安全、合规和用户授权评审。

## 1. 安全边界与威胁模型

需要重点防范：跨用户越权、账号撞库、插件 Token 泄露、恶意文件、简历隐私泄露、AI 供应商过度收集、任务重放、多个设备重复投递、日志泄密、管理员滥权，以及招聘平台验证码/风控被错误自动化处理。

系统采用两种身份：

- Web 用户：通过安全 Cookie Session 使用后台。
- 插件设备：通过受限 Scope 的不透明插件 Token 使用插件专属接口。

两种身份不能互换。插件无权读取简历全文、修改求职目标、确认任务、查看额度或使用后台管理能力。

## 2. 用户数据隔离

1. `users` 之外的每张用户业务表直接包含 `user_id`。
2. API 从认证上下文获取 `user_id`，不接受客户端指定或覆盖。
3. 查询和更新始终同时使用 `user_id + resource_id`；越权访问统一返回 404，避免暴露资源是否存在。
4. 表之间采用包含 `user_id` 的复合外键，阻止跨用户错误关联。
5. 对 PostgreSQL 启用 Row Level Security 作为纵深防御，并用非超级用户、非 BYPASSRLS 的应用角色连接。
6. Redis Key、对象存储 Key、缓存和队列消息同样带服务端可信租户维度；缓存 Key 不能只使用资源 ID。
7. 批量查询、导出、统计、后台任务和管理员接口必须做与单条接口相同的隔离测试。

管理员不是默认“可看所有简历”的超级用户。跨用户访问必须使用独立权限、说明理由、最小字段和完整审计。

## 3. 密码安全存储

密码不应“可逆加密后保存”，而应做不可逆密码哈希：

- 优先使用 Argon2id；如使用 BCrypt，成本参数建议不低于当前安全基线并可逐步升级。
- 每个密码使用库自动生成的独立 Salt；可选服务端 Pepper 放密钥管理系统，不放数据库或仓库。
- 登录错误统一返回“账号或密码错误”，避免枚举注册邮箱。
- 按 IP、邮箱哈希和账号维度限流，连续失败触发临时锁定和安全审计。
- 登录成功轮换 Session ID；修改密码或账号禁用时撤销相关 Web Session。
- 日志、指标、异常、审计和 APM 永不记录密码请求体。

第一版不做微信登录。后续如增加第三方登录，应使用标准 OIDC/OAuth 流程并单独建身份关联表。

## 4. Web Session、CSRF 与传输安全

- Session ID 使用高熵随机值，服务端状态放 Redis，Cookie 设置 `HttpOnly`、`Secure`、合适的 `SameSite`、明确 Path 和有限有效期。
- 不把长期访问 Token 放在 `localStorage` 或 URL。
- Web 的 POST/PUT/PATCH/DELETE 执行 CSRF 校验；登录和绑定码接口也要防登录 CSRF 和滥用。
- 登录、权限变更等敏感操作后轮换 Session；退出立即使服务端 Session 失效。
- 公网强制 HTTPS，HTTP 只重定向；配置 HSTS、现代 TLS 和证书自动续期告警。
- Nginx、Web、API 到外部对象存储和 AI 服务的连接也使用 TLS；PostgreSQL/Redis 不暴露公网。

## 5. 插件 Token 哈希存储

- 插件 Token 使用至少 256 bit CSPRNG 随机值，不使用可预测 UUID 作为唯一秘密。
- 明文 Token 只在绑定成功时返回一次；数据库仅存 `token_prefix` 和 SHA-256/HMAC-SHA-256 哈希。
- 比较哈希时使用常量时间比较；Token 有明确 Scope、设备 ID、用户 ID、过期时间和状态。
- 插件只把 Token 存在 `chrome.storage.local`，不使用 `chrome.storage.sync`，不写页面 DOM、日志、URL 或剪贴板。
- API 日志和反向代理日志必须过滤 `Authorization` Header。
- 支持 Token 轮换；发现泄露、设备撤销、用户禁用或版本不兼容时立即拒绝旧 Token。

随机高熵 Token 的哈希可直接用于定位；如果引入服务端 Pepper，必须规划密钥轮换和灾备恢复。

## 6. 插件设备绑定与撤销

绑定流程：

1. 已登录 Web 用户主动申请一次性绑定码。
2. 绑定码放 Redis，建议 5 分钟过期、限制活动数量、使用一次立即失效。
3. 插件提交绑定码、随机安装 ID、显示名、浏览器/扩展版本和能力列表。
4. 服务端从绑定码得到 `user_id`，创建设备和 Token 哈希；不接受插件提交的 `user_id`。
5. Web 后台展示设备最近在线时间、版本和撤销按钮。

撤销要求：

- 撤销设备时立即撤销该设备全部 Token。
- 清理尚未开始的设备任务分配，释放过期/活动租约；已完成历史事件保留。
- 被撤销设备调用任何插件 API 均返回 `DEVICE_REVOKED`。
- 不采集 MAC 地址、硬盘序列号等硬件指纹；只使用插件生成的随机安装 ID 哈希。

## 7. 招聘平台账号、密码和 Cookie 禁止入库

服务器和 Cloud 数据库没有招聘平台账号、密码、Cookie、LocalStorage、SessionStorage、扫码登录票据或浏览器 Profile 对应字段。

- 用户只在招聘平台官网和自己的 Chrome/Edge 中登录。
- 插件复用浏览器原生会话，但不得读取并上传 Cookie。
- 禁止提供 Cookie 保存、导入、导出、同步或“云端托管登录”接口。
- 禁止把当前本地版 `CookieService`、`cookie` 表、保存 Cookie API 和 Playwright 用户目录迁入 Cloud 服务。
- 调试包、错误报告和截图默认也不能包含 Cookie、二维码、手机号或招聘平台个人资料。

发现请求中包含疑似 Cookie/Token 字段时，API 应拒绝、记录脱敏安全事件，并不把原值写入日志。

## 8. 简历文件安全

简历属于高敏感个人资料：

- 只允许明确格式和大小，校验扩展名、MIME 和文件魔数，限制 PDF 页数、压缩包展开大小和解析时间。
- 对上传文件进行恶意内容扫描；解析在低权限、受限 CPU/内存/网络的隔离进程中进行。
- 原文件存私有卷或私有 Bucket，使用随机对象 Key；Nginx 不直接暴露上传目录。
- 静态加密使用云盘/对象存储服务端加密；提取文本可做应用层字段加密，密钥进入 KMS/密钥管理。
- 下载使用短期签名 URL 或 API 鉴权流式返回，响应使用 `Content-Disposition: attachment`、`nosniff` 和正确 MIME。
- 文件名只作展示，清理路径分隔符和控制字符，不用原文件名拼接磁盘路径。
- 用户删除简历后，数据库立即不可见，后台可验证地删除原文件、预览、提取文本和缓存；备份按保留周期到期清理。
- 非生产环境只使用虚构简历；真实简历不进入 Git、CI Artifact、测试夹具或公开 Issue。

## 9. AI 请求最小化与脱敏

- AI Worker只读取当前分析所需的简历文本、求职目标和岗位字段，不上传原始文件。
- 默认移除或替换姓名、电话、邮箱、证件号、精确地址、照片链接、账号标识和无关经历细节。
- Prompt 模板版本化，记录模型、耗时、Token 用量和结果摘要，不记录完整简历或完整 Prompt 到普通日志。
- 对供应商选择进行数据处理条款、数据驻留、保留期限和“是否用于训练”评审；优先选择可关闭训练和短保留的企业 API。
- 模型 API Key 由服务器密钥管理，不传给 Web 或插件；不同环境使用不同 Key 并定期轮换。
- 模型返回视为不可信输入：限制长度、严格解析结构化字段、转义展示、禁止执行其中的链接/脚本/命令。
- AI 结果只是建议，不能直接触发确认或投递；失败不默认降级为“建议投递”。

## 10. 操作日志与审计

需要审计的关键动作包括：注册/登录/失败/锁定、简历上传和删除、求职目标更新、插件绑定/撤销/Token 轮换、岗位批量上传、AI 分析、任务创建/确认/跳过、插件领取/成功/失败/暂停、额度调整和管理员跨用户操作。

要求：

- 使用稳定动作代码、actor、目标 ID、结果、时间和 `request_id`。
- 任务状态同时写不可变 `delivery_task_events`；安全和管理操作写 `audit_logs`。
- 日志字段使用白名单，过滤 `Authorization`、Cookie、密码、简历全文、完整岗位描述、完整 Prompt/响应和堆栈中的敏感参数。
- 普通日志设置短保留；安全审计和额度流水按明确期限保存。访问审计日志本身也需要授权和审计。
- 告警包括跨用户访问拒绝激增、登录撞库、绑定码爆破、异常采集速率、任务重放、AI 费用突增和管理员高风险操作。

## 11. CORS、CSP 与来源校验

- Web 和 API 优先部署同站点以减少 CORS；若分域，只允许精确的生产/预发布 Web Origin。
- 使用 Cookie 的 CORS 响应不得配置 `Access-Control-Allow-Origin: *`，并仅允许需要的 Method/Header。
- 插件 API 精确允许已发布扩展 ID 对应的 `chrome-extension://<extension-id>`；开发扩展 ID 使用独立开发环境白名单。
- 服务端不能仅信任 `Origin` 做鉴权，仍必须验证 Session/CSRF 或插件 Token。
- Web 配置严格 CSP、`frame-ancestors`、`X-Content-Type-Options: nosniff`、合理 Referrer Policy 和 Permissions Policy。
- Nginx/应用校验 Host，重置用户传入的转发头，避免 Host Header 和 IP 伪造。

## 12. Rate Limit 与滥用控制

Redis 中按接口风险设置多维限流：

| 接口类型 | 维度 | 策略重点 |
| --- | --- | --- |
| 注册/登录 | IP、邮箱哈希、账号 | 慢速爆破、指数退避、临时锁定 |
| 绑定码生成/绑定 | 用户、IP、绑定码 | 5 分钟 TTL、尝试次数、单次消费 |
| 简历上传 | 用户、IP、文件体积 | 次数、并发和存储配额 |
| 岗位采集 | 用户、设备、平台 | 每秒/分钟/日限制和批量上限 |
| AI 分析 | 用户、套餐、队列 | 并发、日额度、幂等和成本保护 |
| 插件任务轮询 | 设备 | 返回 `pollAfterSeconds`，空轮询退避 |
| 任务状态回传 | 用户、设备、任务 | 租约、execution ID、幂等键防重放 |

返回 `429` 和合理 `Retry-After`，但不暴露全局容量。限流不能替代平台规则；插件自身也要限制采集和投递节奏。

## 13. Chrome Extension 最小权限

Manifest V3 的 `host_permissions` 只声明：

- Cloud API 的精确 HTTPS 域名，例如 `https://app.example.com/api/*`。
- 当前实际支持的 Boss、智联等招聘平台精确域名模式。
- 如 Web 页面需要与插件桥接，仅声明正式 Web 域名；开发版单独包含 localhost。

安全要求：

- 禁止 `<all_urls>`，新增平台域名必须经过代码评审、隐私说明更新和用户可见授权。
- 评估使用 `optional_host_permissions`，让用户只启用需要的平台。
- 不请求 `cookies`、`history`、`webRequestBlocking` 等非必要高风险权限。
- content script 只注入受支持页面，消息处理校验 `sender.id`、`sender.url`、消息类型、Schema 和请求 ID。
- 不执行服务器下发的任意 JavaScript/选择器脚本；插件逻辑随签名扩展版本发布。
- 不把招聘平台页面数据发送给第三方；只向 Cloud 上传字段白名单内的岗位结构。
- 生产扩展和开发扩展使用不同后端环境、ID、密钥和发布通道。

当前本地版清单已按 localhost、Boss、智联列域名，这是可复用思路；Cloud 必须替换 localhost 信任模型并重新审核每个权限。

## 14. 用户确认机制

- 创建投递任务只能进入 `PENDING_CONFIRMATION`，AI 或插件不能直接写 `CONFIRMED`。
- 确认必须来自已登录 Web 用户的明确动作，并记录 `confirmed_at`、`confirmed_by`、内容版本和事件。
- 确认界面显示平台、公司、岗位、链接、最终招呼语和将使用的插件设备。
- 修改岗位目标、招呼语或其他实质性执行内容后，旧确认失效并回到待确认。
- 插件 pending 接口只返回 `CONFIRMED` 任务；领取采用设备绑定、短租约、乐观锁和幂等键。
- “批量确认”如后续开放，必须逐项展示、限制条数、明确二次确认并保存每条确认记录，不能默认全选或后台静默确认。
- 用户可以在尚未执行时取消/跳过；执行中状态应先让插件安全停止再结束任务。

## 15. 验证码、登录失效和风控必须暂停

插件一旦检测到下列情况必须停止当前自动步骤，不得继续点击或自动重试：

- 验证码、滑块、人机校验或扫码确认。
- 登录页面、会话过期、账号异常或需要重新认证。
- 风控、频率限制、账号限制、异常访问或平台警告。
- 页面结构未知、关键按钮含义不明、简历选择需要用户判断。

插件调用 `pause` 回传受控原因码：`CAPTCHA_REQUIRED`、`LOGIN_REQUIRED`、`RISK_CONTROL`、`PAGE_CHANGED`、`USER_ACTION_REQUIRED`。Web 明确提示用户在原招聘平台页面人工处理；处理完成后用户再次查看任务并重新确认。服务器和插件都不自动解决验证，也不使用第三方打码服务。

## 16. 不支持绕过平台限制

产品明确不提供：

- 验证码识别、滑块模拟、设备指纹伪造或风控对抗。
- 代理池、账号池、集中批量登录或 Cookie 共享。
- 绕过招聘平台频率、权限、付费墙、地域或账号限制。
- 隐藏自动化行为、未确认批量投递、页面后台静默代投。
- 服务器直接抓取或投递招聘平台。

平台条款或页面发生变化时，应暂停相关能力、评估合规性和更新适配，而不是通过规避措施维持运行。

## 17. 密钥、配置与供应链

- `.env`、生产配置、证书私钥、数据库密码、AI Key、Session 密钥和 Token Pepper 不进入 Git。
- 仓库只保留无真实值的 `.env.example`；CI 日志对秘密做 Mask。
- 使用部署平台 Secret/KMS，按环境隔离并定期轮换；最小化容器和人员访问权限。
- CI 执行依赖锁定、SCA、CodeQL、秘密扫描、镜像扫描和 SBOM 生成。
- 第三方依赖和 Chrome Web Store 发布采用受保护账号和多因素认证。
- Docker 镜像使用非 root 用户、只读文件系统（可行处）、固定基础镜像版本和最少包。

## 18. 备份、删除与事件响应

- PostgreSQL、对象存储和密钥配置分别备份并定期演练恢复；Redis 不作为唯一事实来源。
- 明确账号、简历、岗位、任务、审计和备份的保留期限；用户删除请求可追踪到实际清理完成。
- 安全事件流程包含：隔离、撤销 Session/Token、保留脱敏证据、评估受影响用户、修复、通知和复盘。
- 发现插件 Token 泄露可单设备撤销；发现系统密钥泄露要全量轮换并评估历史数据影响。
- 安全问题通过私密渠道报告，不在公开 Issue 粘贴 Token、简历、日志或截图。

## 19. 发布前安全验收

- [ ] A/B 用户所有单条、批量、导出和异步接口的隔离测试通过。
- [ ] 密码只存强哈希，Session/CSRF/Cookie 属性和登录限流通过测试。
- [ ] 插件 Token 明文不入库、不入日志，设备撤销立即生效。
- [ ] 数据库、API、插件清单均不存在招聘平台密码或 Cookie 通道。
- [ ] 简历上传、恶意文件、私有访问、删除和备份清理完成演练。
- [ ] AI 输入脱敏，供应商数据策略经过评审，模型输出按不可信输入处理。
- [ ] CORS 是精确白名单，CSP 和 Nginx 安全头有效。
- [ ] 插件没有 `<all_urls>` 和非必要高风险权限。
- [ ] 未确认任务无法被插件领取；并发领取只成功一次。
- [ ] 验证码、登录失效、风控和未知页面都进入暂停，不自动绕过。
- [ ] CI、CodeQL、依赖/镜像/秘密扫描没有未处理高危项。
- [ ] 数据备份恢复、用户删除和安全事件响应至少演练一次。
