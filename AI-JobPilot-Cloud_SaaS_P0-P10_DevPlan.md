# AI-JobPilot-Cloud SaaS 开发流程验收与后续开发文档

> 适用项目：`AI-JobPilot-Cloud`<br>
> 当前进度假设：已经开发到 **P9：额度系统 + 基础后台**<br>
> 本文用途：直接交给 Codex，用于 **验证已完成轮次、修复遗漏问题、继续开发 P10 腾讯云上线前验收**。<br>
> 重要原则：不要影响原本地版 `AI-JobPilot` 仓库。当前仓库应当是独立的 SaaS 云端版仓库。

---

## 0. 给 Codex 的总要求

请你作为资深全栈架构师，基于当前 `AI-JobPilot-Cloud` 最新代码，完成一次从 P0 到 P9 的全链路验收，并继续推进 P10。

当前项目目标是将投递牛马从本地单机工具演进为：

```text
云端 SaaS 求职投递工作台 + 用户浏览器插件执行器
```

核心职责边界：

```text
云端服务器：
用户系统、简历、求职目标、岗位池、AI 匹配、投递清单、插件任务、额度系统、基础后台、审计日志、备份恢复。

浏览器插件：
在用户自己的 Chrome / Edge 中采集岗位、上传岗位、拉取用户已确认的任务、在用户浏览器环境中执行、回传结果。
```

必须坚持：

1. 服务器不保存招聘平台账号密码。
2. 服务器不保存招聘平台 Cookie。
3. 服务器不上传或保存用户浏览器缓存。
4. 投递动作必须发生在用户自己的浏览器环境里。
5. 用户确认前，插件不能执行投递。
6. 遇到验证码、登录失效、平台风控、页面异常，必须暂停或失败记录，禁止绕过。
7. 所有用户数据必须按 `user_id` 隔离。
8. 所有敏感信息必须脱敏或哈希存储。
9. 不要提交 `.env`、数据库、日志、真实简历、Cookie、Token、API Key。
10. 每一轮修改必须有测试、构建、提交说明。

---

## 1. 开始前必须执行的检查

在任何修改前，先执行：

```bash
git status
```

请确认：

1. 当前仓库是 `AI-JobPilot-Cloud`，不是本地版 `AI-JobPilot`。
2. 当前分支不是 `main`。如果在 `main`，请先创建分支：

```bash
git checkout -b verify/p9-and-p10-launch-check
```

3. 如果发现未提交修改，先说明这些修改属于哪些文件，不要直接覆盖。
4. 检查是否存在不应提交的文件：

```text
.env
.env.local
db/
data/
logs/
output/
target/
front/.next/
front/node_modules/
node_modules/
chrome-profile/
cookie.json
*.db
*.log
真实简历
API Key
Token
Cookie
浏览器缓存
```

如发现以上文件，应删除或加入 `.gitignore`，不得提交。

---

## 2. 当前开发轮次总览

当前 SaaS 开发流程按以下轮次推进：

```text
P0：新仓库初始化与 SaaS 定位
P1：SaaS 架构文档与数据库/API/安全设计
P2：腾讯云基础设施、PostgreSQL、Redis、Docker、Nginx
P3：用户系统与多用户权限隔离
P4：简历上传与求职目标配置
P5：岗位池 + AI 匹配
P5.5：云端数据安全补强
P6：投递清单 + 用户确认闭环
P7：插件云端绑定 + 岗位上传
P8：插件拉任务 + 执行结果回传
P9：额度系统 + 基础后台
P10：腾讯云上线前验收 + 备份恢复 + 安全检查
```

当前用户反馈：**已经做到 P9**。<br>
你的任务：

```text
先验收 P0～P9 是否真正完成
再修复关键遗漏
最后推进 P10
```

---

## 3. P0 验收：新仓库初始化与 SaaS 定位

### 为什么做

本地版 `AI-JobPilot` 是 Windows 本地工作台，不适合直接改成公网多人 SaaS。<br>
因此需要新建 `AI-JobPilot-Cloud` 仓库，避免污染本地稳定版。

### 应得到什么

1. 新仓库独立存在。
2. README 明确这是 SaaS 云端版。
3. 原 `AI-JobPilot` 保留为本地版。
4. 新仓库没有携带本地数据库、日志、缓存、密钥、真实简历。

### 验收清单

请检查：

- [ ] README 是否明确写明 `AI-JobPilot-Cloud` 是 SaaS 云端版。
- [ ] README 是否说明原 `AI-JobPilot` 是本地版。
- [ ] `.gitignore` 是否忽略 `.env`、数据库、日志、上传文件、缓存、浏览器资料目录。
- [ ] 仓库中是否没有真实数据库、真实简历、Cookie、Token、API Key。
- [ ] 包名、项目名、文档入口是否已经从本地版语境调整到云端版语境。

---

## 4. P1 验收：SaaS 架构文档与设计

### 为什么做

SaaS 不是把本地工具部署到服务器。<br>
它需要多用户隔离、插件绑定、云端任务、额度系统、备份恢复、安全边界。

### 应得到什么

至少应存在以下文档：

```text
CLOUD_ARCHITECTURE.md
CLOUD_DATABASE_DESIGN.md
CLOUD_API_DESIGN.md
CLOUD_SECURITY.md
CLOUD_ROADMAP.md
CLOUD_DEPLOY_TENCENT.md
CLOUD_BACKUP_RECOVERY.md
CLOUD_PRIVACY_CHECKLIST.md
```

### 验收清单

- [ ] 是否说明云端服务器和浏览器插件的职责边界。
- [ ] 是否说明投递动作必须在用户浏览器执行。
- [ ] 是否说明服务器不保存招聘平台账号密码、Cookie、浏览器缓存。
- [ ] 数据库设计是否包含 users、resumes、job_preferences、job_posts、job_matches、delivery_tasks、plugin_devices、user_quotas、audit_logs。
- [ ] API 设计是否覆盖用户系统、简历、岗位池、AI 匹配、投递任务、插件绑定、插件上传、插件任务、额度后台。
- [ ] 安全文档是否包含密码哈希、token 哈希、CORS、Rate Limit、审计日志、备份恢复、数据删除。

---

## 5. P2 验收：腾讯云基础设施

### 为什么做

既然用户账号、简历、岗位、投递任务会放在腾讯云服务器上，必须先有云端基础设施和安全边界。

### 应得到什么

1. Spring Boot 后端。
2. Next.js 前端。
3. PostgreSQL 主数据库。
4. Redis 缓存 / 任务队列。
5. Docker Compose。
6. Nginx / HTTPS 部署说明。
7. `.env.example` 配置模板。
8. 健康检查接口。

### 验收清单

- [ ] 后端是否连接 PostgreSQL，而不是继续使用 SQLite 作为 SaaS 主数据库。
- [ ] 是否有 Redis 配置。
- [ ] 是否有 Docker Compose 云端版配置。
- [ ] 是否有 Nginx 配置示例。
- [ ] 是否有 HTTPS 部署说明。
- [ ] `.env.example` 是否只有占位符，没有真实密钥。
- [ ] 数据库、Redis、后端内部端口是否不暴露公网。
- [ ] 只应公网开放 80、443、必要时 22。
- [ ] 是否有 `/api/health` 或类似健康检查接口。

---

## 6. P3 验收：用户系统与多用户权限隔离

### 为什么做

SaaS 的第一地基是用户系统。没有用户系统，就无法隔离简历、岗位、AI 结果、投递任务和额度。

### 应得到什么

1. 用户注册。
2. 用户登录。
3. 用户退出。
4. 当前用户接口。
5. 密码哈希存储。
6. JWT / Session 登录态。
7. 用户权限拦截。
8. `user_id` 数据隔离。

### 验收清单

- [ ] 是否有 `users` 表。
- [ ] 密码是否使用 BCrypt、Argon2 或同等级哈希。
- [ ] 数据库中是否没有明文密码。
- [ ] 是否有 `/api/auth/register`。
- [ ] 是否有 `/api/auth/login`。
- [ ] 是否有 `/api/auth/logout`。
- [ ] 是否有 `/api/me`。
- [ ] 登录态是否来自安全 token 或 session。
- [ ] 所有用户数据查询是否自动按当前用户过滤。
- [ ] 是否有越权访问测试。

---

## 7. P4 验收：简历上传与求职目标配置

### 为什么做

AI 匹配必须基于用户自己的简历和求职目标。简历属于敏感数据，必须从第一版就按用户隔离和文件安全设计。

### 应得到什么

1. 简历上传。
2. 简历文本提取。
3. 简历归属当前用户。
4. 求职目标配置。
5. 文件大小和类型限制。
6. 简历查看、删除权限控制。

### 验收清单

- [ ] 是否有 `resumes` 表。
- [ ] 是否有 `job_preferences` 表。
- [ ] 简历文件是否绑定 `user_id`。
- [ ] 简历文本是否绑定 `user_id`。
- [ ] A 用户是否无法访问 B 用户简历。
- [ ] 是否限制上传文件类型，例如 PDF / DOCX / TXT。
- [ ] 是否限制上传大小。
- [ ] 是否避免日志打印完整简历。
- [ ] 求职目标是否包含城市、岗位、薪资、行业、双休、规避关键词、补充要求。

---

## 8. P5 验收：岗位池 + AI 匹配

### 为什么做

岗位池和 AI 匹配是产品价值核心：用户不用一个个看岗位，由系统分析是否值得投。

### 应得到什么

1. 岗位池。
2. 岗位去重。
3. AI 匹配分析。
4. 推荐 / 谨慎 / 不建议。
5. 风险点。
6. 简历命中点。
7. 招呼语。
8. 分析结果入库。

### 验收清单

- [ ] 是否有 `job_posts` 表。
- [ ] 是否有 `job_matches` 表。
- [ ] 岗位是否绑定 `user_id`。
- [ ] 同一用户下，`platform + platform_job_id` 是否去重。
- [ ] 不同用户采集同一个岗位是否不会互相覆盖。
- [ ] AI 分析是否基于当前用户简历、求职目标、岗位信息。
- [ ] A 用户是否无法分析 B 用户岗位。
- [ ] AI 结果是否包含 match_score、match_level、reason、risks、resume_highlights、greeting_message。
- [ ] AI 调用失败是否记录错误原因，不让任务崩掉。
- [ ] AI 调用日志是否不打印完整简历、API Key、Token。

---

## 9. P5.5 验收：云端数据安全补强

### 为什么做

P5 后系统已有账号、简历、岗位、AI 分析结果。如果继续做投递任务和插件执行，安全风险会放大。

### 应得到什么

1. 用户数据隔离检查。
2. 密码哈希检查。
3. 插件 token 哈希。
4. 日志脱敏。
5. 审计日志。
6. CORS 收紧。
7. Rate Limit。
8. 备份恢复文档。
9. 隐私检查清单。

### 验收清单

- [ ] 是否有 `audit_logs` 表。
- [ ] 登录、上传简历、修改偏好、上传岗位、AI 分析、创建任务、插件绑定等是否写审计日志。
- [ ] 日志是否脱敏。
- [ ] CORS 是否不使用 `*`。
- [ ] CORS 是否通过 `APP_ALLOWED_ORIGINS` 配置。
- [ ] 登录、注册、AI 分析、插件上传、插件拉任务是否有 Rate Limit。
- [ ] 是否有 `CLOUD_BACKUP_RECOVERY.md`。
- [ ] 是否有 `CLOUD_PRIVACY_CHECKLIST.md`。
- [ ] 是否有 A 用户访问 B 用户数据的测试。

---

## 10. P6 验收：投递清单 + 用户确认闭环

### 为什么做

AI 匹配结果需要变成用户可操作的投递任务。P6 是从“分析工具”变成“投递工作台”的关键。

### 应得到什么

1. 投递清单。
2. 用户确认机制。
3. 跳过岗位。
4. 编辑招呼语。
5. 任务事件记录。
6. 待插件执行任务池。

### 应支持状态

```text
WAITING_CONFIRM
CONFIRMED
SKIPPED
PAUSED_NEED_USER
```

### 验收清单

- [ ] 是否有 `delivery_tasks` 表。
- [ ] 是否有 `delivery_task_events` 表。
- [ ] 任务是否绑定 `user_id`、`profile_id`、`job_post_id`、`job_match_id`。
- [ ] 是否支持从 `job_match` 创建投递任务。
- [ ] 重复创建同一个 `job_match` 是否幂等。
- [ ] 用户是否可以确认投递。
- [ ] 用户是否可以跳过。
- [ ] 用户是否可以编辑招呼语。
- [ ] A 用户是否无法操作 B 用户任务。
- [ ] 状态变化是否写入 `delivery_task_events`。

---

## 11. P7 验收：插件云端绑定 + 岗位上传

### 为什么做

服务器有岗位池，但真正的岗位来源应该来自用户自己的浏览器插件。P7 让插件和云端账号打通。

### 应得到什么

1. 插件绑定码。
2. 插件设备表。
3. 插件 token。
4. 插件心跳。
5. 单个岗位上传 API。
6. 批量岗位上传 API。
7. 插件解绑。

### 验收清单

- [ ] 是否有 `plugin_devices` 表。
- [ ] 是否有 `plugin_tokens` 表。
- [ ] 是否有 `plugin_bind_codes` 表。
- [ ] 绑定码是否有过期时间。
- [ ] 插件 token 是否只存 hash。
- [ ] 插件 token 是否可撤销。
- [ ] 插件是否可心跳更新 `last_seen_at`。
- [ ] 插件上传岗位时是否由 token 解析 `user_id`，而不是由插件传任意 `user_id`。
- [ ] 重复上传岗位是否去重。
- [ ] 插件是否不上传 Cookie、账号密码、浏览器缓存。
- [ ] host_permissions 是否不使用 `<all_urls>`。

---

## 12. P8 验收：插件拉任务 + 执行结果回传

### 为什么做

P8 才是浏览器执行器闭环。插件只能拉取用户已经确认的任务，并在用户自己的浏览器中执行。

### 应得到什么

1. 插件拉取待执行任务。
2. 任务状态流转。
3. 执行成功回传。
4. 执行失败回传。
5. 需要用户处理回传。
6. 失败类型统一。
7. 异常暂停机制。

### 应支持状态

```text
WAITING_CONFIRM
CONFIRMED
PULLED_BY_PLUGIN
RUNNING
SUCCESS
FAILED
SKIPPED
PAUSED_NEED_USER
```

### 失败类型

```text
LOGIN_REQUIRED
CAPTCHA_REQUIRED
RISK_CONTROL
JOB_EXPIRED
BUTTON_NOT_FOUND
PAGE_STRUCTURE_CHANGED
NETWORK_ERROR
UNKNOWN_ERROR
```

### 验收清单

- [ ] 插件只能拉取当前用户 `CONFIRMED` 任务。
- [ ] 插件不能拉取 `WAITING_CONFIRM` 任务。
- [ ] 插件 start 后是否变为 `RUNNING`。
- [ ] success 后是否变为 `SUCCESS`。
- [ ] fail 后是否变为 `FAILED`。
- [ ] pause 后是否变为 `PAUSED_NEED_USER`。
- [ ] 被撤销 token 是否不能拉任务。
- [ ] A 用户插件是否不能更新 B 用户任务。
- [ ] 状态变化是否写入 `delivery_task_events`。
- [ ] 遇到验证码、登录失效、风控是否暂停。
- [ ] 是否禁止绕过验证码和登录。
- [ ] 是否禁止上传 Cookie。

---

## 13. P9 验收：额度系统 + 基础后台

### 为什么做

P8 后产品主链路已经具备：岗位上传、AI 匹配、用户确认、插件执行、结果回传。P9 开始做商业化底座。

### 应得到什么

1. 免费额度。
2. AI 分析额度。
3. 投递额度。
4. 额度消耗记录。
5. 基础后台。
6. 用户列表。
7. 失败任务查看。
8. 插件设备状态。

### 验收清单

- [ ] 是否有 `user_quotas` 表。
- [ ] 是否有 `quota_usage_logs` 表。
- [ ] 是否预留 `subscriptions` 表。
- [ ] 免费用户是否有默认额度。
- [ ] AI 分析前是否检查 analysis quota。
- [ ] 确认投递或创建投递任务前是否检查 delivery quota。
- [ ] 额度不足是否返回明确提示。
- [ ] 重复请求是否不会重复扣额度。
- [ ] 扣减是否写入 `quota_usage_logs`。
- [ ] 是否有 `/api/quota/me`。
- [ ] 是否有基础 admin 页面或 admin API。
- [ ] 普通用户是否不能访问后台。
- [ ] admin 是否可以查看用户、额度、岗位数、AI 分析数、任务数、插件设备、失败任务。
- [ ] 后台是否不展示完整密码、token、API Key、Cookie。

---

## 14. 当前最重要任务：P9 全量验收与补漏

请你现在优先完成：

```text
P0～P9 全链路验收
尤其重点验收 P5.5、P6、P7、P8、P9
```

如果发现遗漏，按优先级修：

### P0 级必须修

```text
用户数据越权
密码明文
token 明文
数据库或 Redis 暴露公网
.env 泄露
日志打印敏感信息
插件可操作未确认任务
插件可操作其他用户任务
额度可绕过
普通用户可访问后台
```

### P1 级应尽快修

```text
审计日志缺失
Rate Limit 缺失
备份恢复文档不完整
任务状态事件不完整
失败类型不统一
插件解绑不完整
后台统计缺少关键数据
```

### P2 级可后续优化

```text
后台 UI 美化
更复杂套餐
真实支付
邮件通知
微信提醒
高级数据分析
更精细的 AI 成本统计
```

---

## 15. P10 开发：腾讯云上线前验收 + 备份恢复 + 安全检查

如果 P0～P9 验收通过或关键问题已修复，请继续进入 P10。

### 为什么现在做

P9 完成后，产品已经具备基本商业化雏形。只要要放真实用户数据，上线前必须完成部署、安全、备份、隐私、回滚、数据删除和小范围测试标准。

### 做完之后得到什么

1. 腾讯云部署文档。
2. 上线检查清单。
3. 数据库备份恢复演练。
4. HTTPS 检查。
5. 安全组检查。
6. 环境变量检查。
7. 日志脱敏检查。
8. 用户数据删除方案。
9. 小范围测试用户上线标准。

### 给 Codex 的 P10 开发要求

请开始 SaaS 第 P10 轮：腾讯云上线前验收 + 备份恢复 + 安全检查。

本轮目标：

完成腾讯云上线前验收，包括部署文档、环境变量检查、HTTPS、安全组、数据库备份恢复、日志脱敏、用户数据删除方案和小范围测试标准。本轮不开发新业务功能。

#### 15.1 开始前检查

1. 执行：

```bash
git status
```

2. 确认当前仓库是 `AI-JobPilot-Cloud`。
3. 确认当前分支不是 `main`。如果在 `main`，请新建分支：

```bash
git checkout -b chore/p10-cloud-launch-check
```

4. 不要覆盖已有未提交修改。
5. 不要提交 `.env`、数据库、日志、真实简历、Cookie、Token、API Key。

#### 15.2 腾讯云部署文档

请新增或完善：

```text
CLOUD_DEPLOY_TENCENT.md
CLOUD_BACKUP_RECOVERY.md
CLOUD_LAUNCH_CHECKLIST.md
CLOUD_PRIVACY_CHECKLIST.md
```

`CLOUD_DEPLOY_TENCENT.md` 需要写清楚：

1. 腾讯云 CVM 基础环境。
2. Docker 和 Docker Compose 安装。
3. Nginx 配置。
4. HTTPS 证书配置。
5. PostgreSQL 配置。
6. Redis 配置。
7. 上传目录配置。
8. 日志目录配置。
9. `.env` 配置。
10. 如何启动服务。
11. 如何查看日志。
12. 如何回滚版本。

#### 15.3 安全组和端口

请在文档和配置中明确：

公网只开放：

```text
80
443
22，建议限制固定 IP
```

不得公网暴露：

```text
PostgreSQL 5432
Redis 6379
后端内部端口
前端开发端口
Docker 内部服务端口
```

#### 15.4 环境变量检查

请整理 `.env.example`，确保包含：

```text
DATABASE_URL
DATABASE_USERNAME
DATABASE_PASSWORD
REDIS_URL
JWT_SECRET
APP_ALLOWED_ORIGINS
APP_PUBLIC_URL
AI_API_KEY
AI_BASE_URL
AI_MODEL
UPLOAD_DIR
LOG_DIR
PLUGIN_TOKEN_SECRET
RATE_LIMIT 配置
ADMIN 初始化配置
```

要求：

1. `.env.example` 只能放占位符。
2. 不允许出现真实密钥。
3. 文档中提醒不要提交 `.env`。

#### 15.5 备份和恢复

请完善 `CLOUD_BACKUP_RECOVERY.md`。

必须包括：

1. PostgreSQL 手动备份命令。
2. PostgreSQL 自动备份方案。
3. PostgreSQL 恢复命令。
4. 恢复演练步骤。
5. 上传文件备份方案。
6. Redis 数据是否需要持久化。
7. 备份文件权限。
8. 备份文件加密建议。
9. 备份失败告警建议。
10. 多久演练一次恢复。

#### 15.6 上线前安全检查

请完善 `CLOUD_LAUNCH_CHECKLIST.md`。

至少包括：

1. HTTPS 是否可用。
2. HTTP 是否自动跳转 HTTPS。
3. CORS 是否只允许正式域名。
4. Rate Limit 是否开启。
5. 密码是否哈希。
6. 插件 token 是否哈希。
7. JWT_SECRET 是否足够随机。
8. 数据库是否不暴露公网。
9. Redis 是否不暴露公网。
10. 日志是否脱敏。
11. `.env` 是否未提交。
12. 备份是否可恢复。
13. 普通用户是否不能访问后台。
14. A 用户是否不能访问 B 用户数据。
15. 插件是否不能上传 Cookie。
16. 遇到验证码是否暂停。
17. 用户是否可以删除自己的数据。
18. 是否有隐私政策和用户协议草案。

#### 15.7 用户数据删除方案

请新增或完善用户数据删除能力或文档方案。

要求说明：

1. 用户删除账号时删除哪些数据。
2. 简历文件如何删除。
3. 岗位记录如何删除。
4. 投递任务如何删除或匿名化。
5. 审计日志是否保留，保留多久。
6. 备份中的数据如何处理。
7. 删除操作如何防误触。

#### 15.8 测试要求

请运行完整检查：

后端：

```bash
./gradlew test
./gradlew build
```

前端：

```bash
cd front
pnpm lint
pnpm build
```

Docker：

```bash
docker compose config
```

如果有部署脚本，请做语法检查。

#### 15.9 P10 输出要求

完成后请告诉我：

1. 本轮修改摘要。
2. 修改了哪些文件。
3. 腾讯云部署方式。
4. 上线前必须人工确认的事项。
5. 数据库备份恢复方式。
6. 安全组应该开放哪些端口。
7. 测试结果。
8. 是否可以进入小范围测试。
9. 还有哪些不能给真实用户使用的风险。

注意：

本轮不开发新业务功能，不接真实支付，不导入真实用户数据，不上传真实简历，不提交任何密钥。

---

## 16. 全链路回归测试清单

请在 P10 前后至少完成以下回归测试。

### 16.1 用户系统

- [ ] 注册成功。
- [ ] 登录成功。
- [ ] 登录失败有明确提示。
- [ ] 密码不明文保存。
- [ ] `/api/me` 返回当前用户。
- [ ] 未登录不能访问受保护接口。

### 16.2 简历和求职目标

- [ ] 用户可以上传简历。
- [ ] 用户可以查看自己的简历状态。
- [ ] 用户可以删除自己的简历。
- [ ] A 用户不能看 B 用户简历。
- [ ] 用户可以保存求职目标。
- [ ] A 用户不能修改 B 用户求职目标。

### 16.3 岗位池和 AI 匹配

- [ ] 用户可以新增岗位。
- [ ] 插件可以上传岗位。
- [ ] 同一用户重复岗位不会重复入库。
- [ ] 不同用户同一岗位互不覆盖。
- [ ] 用户可以触发 AI 分析。
- [ ] AI 分析结果入库。
- [ ] AI 失败有错误记录。
- [ ] A 用户不能分析 B 用户岗位。

### 16.4 投递清单

- [ ] 可以从 AI 匹配结果创建投递任务。
- [ ] 重复创建任务幂等。
- [ ] 用户可以确认任务。
- [ ] 用户可以跳过任务。
- [ ] 用户可以编辑招呼语。
- [ ] 状态变化写入事件表。
- [ ] A 用户不能操作 B 用户任务。

### 16.5 插件绑定和任务

- [ ] 用户可以生成绑定码。
- [ ] 插件可以绑定账号。
- [ ] token 只存 hash。
- [ ] 插件可以心跳。
- [ ] 插件可以上传岗位。
- [ ] 插件可以拉取已确认任务。
- [ ] 插件不能拉取未确认任务。
- [ ] 插件可以回传 success / fail / pause。
- [ ] 被撤销插件不能继续调用接口。

### 16.6 额度和后台

- [ ] 免费用户默认额度正确。
- [ ] AI 分析扣额度。
- [ ] 投递任务扣额度。
- [ ] 额度不足禁止操作。
- [ ] 重复请求不重复扣额度。
- [ ] 普通用户不能访问后台。
- [ ] admin 可以访问后台。
- [ ] 后台不展示敏感字段明文。

### 16.7 云端安全

- [ ] CORS 不使用 `*`。
- [ ] Rate Limit 开启。
- [ ] 日志脱敏。
- [ ] `.env` 未提交。
- [ ] 数据库不暴露公网。
- [ ] Redis 不暴露公网。
- [ ] HTTPS 可用。
- [ ] HTTP 自动跳转 HTTPS。
- [ ] 数据库备份可恢复。

---

## 17. 最终提交要求

完成 P9 验收、补漏和 P10 后，请按以下格式输出总结：

```text
一、本轮总览
- 当前完成到哪一轮
- 本次主要做了什么
- 是否可以进入小范围测试

二、P0～P9 验收结果
- 已通过项
- 修复项
- 未完成项
- 风险项

三、P10 完成情况
- 新增/修改文档
- 新增/修改配置
- 备份恢复方案
- 腾讯云部署方案
- 上线检查清单

四、测试结果
- 后端测试
- 后端构建
- 前端 lint
- 前端构建
- Docker 配置检查
- 插件校验

五、不能上线的风险
- 必须明确列出

六、下一步建议
- 小范围测试前还需要做什么
- 是否可以邀请真实用户
- 是否需要先用测试数据跑一轮
```

最后提交：

```bash
git add .
git commit -m "验证：完成P9验收并补充P10上线检查"
```

如果存在较多修复，可以拆成多个 commit，但每个 commit 信息必须清晰。

---

## 18. 禁止事项

在本轮验证和后续开发中，禁止：

1. 直接在 `main` 上大改。
2. 提交 `.env`。
3. 提交数据库文件。
4. 提交日志。
5. 提交真实简历。
6. 提交 Cookie、Token、API Key。
7. 保存招聘平台账号密码。
8. 上传招聘平台 Cookie 到服务器。
9. 绕过验证码。
10. 绕过登录。
11. 无用户确认批量投递。
12. 普通用户访问后台。
13. A 用户访问 B 用户数据。
14. 数据库或 Redis 暴露公网。
15. 假装测试通过。
