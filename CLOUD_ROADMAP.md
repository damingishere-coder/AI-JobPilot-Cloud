# AI-JobPilot-Cloud SaaS 落地路线图

本文把“投递牛马 SaaS 云端版”拆成阶段 0-9。每个阶段都必须在验收通过后再进入下一阶段；未完成安全和隔离门槛时，不以“先上线再补”为理由跳过。

## 总体原则

- `AI-JobPilot` 本地版保持稳定，`AI-JobPilot-Cloud` 使用独立仓库、版本、Issue、PR 和发布流程。
- 云端负责账号、资料、岗位、AI、投递清单、插件任务、额度和后台；插件负责用户浏览器内的采集和确认后投递。
- 服务器不保存招聘平台账号、密码或 Cookie，不直接登录招聘平台。
- 第一版坚持模块化单体 API + 独立 AI Worker，不提前拆大量微服务。
- 每阶段都补自动化测试、文档、回滚方案和安全检查，不把测试集中拖到最后。

## 阶段依赖

```text
0 冻结本地版
  -> 1 建立 Cloud 独立仓库
    -> 2 云基础设施
      -> 3 用户系统与隔离
        -> 4 简历与求职目标
          -> 5 岗位池与 AI 匹配
            -> 6 投递清单与确认
              -> 7 插件绑定与岗位上传
                -> 8 插件投递与结果回传
                  -> 9 额度、基础付费与后台
```

阶段 6 可以先用模拟插件验证任务状态机；阶段 7-8 再接入真实浏览器闭环。

## 阶段 0：冻结本地稳定版

### 目标

为 Cloud 选定一个可追溯、可测试、可回退的本地稳定版基线，同时保证本地版用户不受 SaaS 改造影响。

### 具体功能/工作

- 清理或记录本地版未提交修改，确认稳定分支和最新 Release。
- 运行后端测试、前端 lint/build、Chrome Extension 校验和关键手工流程。
- 打基线 Tag，记录提交 SHA、版本、测试结果、已知问题和运行依赖。
- 发布或归档本地稳定包，明确本地版维护和安全修复策略。
- 建立两仓库变更移植原则：只选择性移植通用修复，不做自动双向合并。

### 涉及模块

本地版 Git/Release、Spring Boot、Next.js、SQLite/Flyway、Chrome Bridge、CI、CodeQL、Docker 和发布文档。

### 验收标准

- 基线 Tag 和 SHA 可从远端获取，工作区状态有记录。
- CI 和可用构建检查通过；未通过项有明确原因、影响和负责人。
- 本地版安装/启动/关键求职流程可按文档复现。
- 真实 `.env`、数据库、Cookie、日志、浏览器 Profile 和真实简历不在 Git 中。
- 本地版 README 明确它仍是受维护的稳定单机版。

### 不做什么

- 不在本地版直接加入 SaaS 用户表、PostgreSQL 或云端鉴权。
- 不为“代码统一”重构大批本地业务。
- 不删除本地版仓库、Release 或用户数据。

## 阶段 1：新建 AI-JobPilot-Cloud 仓库

### 目标

从冻结基线建立独立 Cloud 仓库和安全开发基线，不复制本地运行数据和 Git 历史包袱。

### 具体功能/工作

- 用 `git archive` 等“只导出已跟踪文件”的方式创建新目录。
- 排除 `.env`、`db/`、`data/`、`logs/`、`output/`、构建缓存、浏览器 Profile 和真实简历。
- 初始化独立 Git 历史、`main` 保护、分支/Commit/PR 规则。
- 更新 README、架构、安全、数据库、API 和路线图定位。
- 建立 CI、CodeQL、依赖扫描、秘密扫描和基础 Docker 构建。
- 创建“复用/重构/删除”迁移清单，并记录本地版基线 SHA。

### 涉及模块

Git/GitHub、README、`.gitignore`、`.dockerignore`、CI、CodeQL、Issue/PR 模板、项目文档。

### 验收标准

- Cloud `origin` 只指向 `AI-JobPilot-Cloud`，与本地版仓库相互独立。
- 新仓库 `main` 首次提交不含敏感信息和运行数据。
- README 准确说明 Cloud 当前尚未具备生产 SaaS 能力。
- CI 可构建基线，秘密扫描无未处理结果。
- 文档明确服务器/插件边界和第一版非目标。

### 不做什么

- 不在本阶段实现用户系统、改数据库或发布生产服务。
- 不把旧仓库 `.git` 目录和全部历史直接复制过来。
- 不自动合并初始化 PR，不删除原仓库或分支。

## 阶段 2：SaaS 基础设施（PostgreSQL、Redis、Docker、Nginx）

### 目标

建立可重复启动、可观察、可备份的 Cloud 开发/测试环境，为业务开发提供可靠底座。

### 具体功能/工作

- 将运行配置分为 dev/test/prod，所有真实密钥通过环境或 Secret 注入。
- 新建 PostgreSQL 连接、Cloud 专用 Flyway 空基线和集成测试容器。
- 引入 Redis 基础设施，为后续 Session、绑定码、限流和 Redis Streams 队列预留连接；本阶段不提前实现业务消费者。
- 把 API 与 AI Worker 设计为同代码库的两个独立进程/容器。
- 配置 Next.js Web、Spring Boot API、Worker、PostgreSQL、Redis 的 Docker Compose。
- Nginx 统一入口、HTTPS、安全头、上传大小和超时限制。
- 建立私有文件存储抽象：开发私有卷，生产可切换 S3 兼容对象存储。
- 增加 liveness/readiness、结构化脱敏日志、请求 ID、指标和数据库备份脚本。

### 涉及模块

Spring Boot 配置和依赖、Flyway、Redis、Worker 启动器、Dockerfile/Compose、Nginx、文件存储抽象、日志和监控。

### 验收标准

- 一条文档化命令能启动全部开发组件，健康检查正确区分存活和就绪。
- PostgreSQL/Redis 不暴露公网，Nginx 是唯一入口。
- Flyway 可在空 PostgreSQL 完成迁移，SQLite SQL 未被直接混用。
- 停止 Redis 后业务事实不丢失；未完成 AI 消息重新投递随真实队列消费者在阶段 5 验收。
- HTTPS、Secret 注入、日志脱敏、备份和恢复至少完成一次测试。

### 不做什么

- 不提前上 Kubernetes、Service Mesh、分布式事务或多地域容灾。
- 不拆大量微服务。
- 不迁移本地 SQLite 中的真实用户数据。

## 阶段 3：用户系统和多用户隔离

### 目标

实现安全的邮箱注册登录和贯穿数据库、API、缓存、文件、队列的 `user_id` 隔离。

### 具体功能/工作

- 实现 `users`、`user_profiles`、Web Session、注册、登录、退出和 `/api/me`。
- 使用 Argon2id/BCrypt 密码哈希、Cookie 安全属性、CSRF 和会话轮换。
- 建立 IP/邮箱/账号限流、临时锁定、账号禁用和安全审计。
- 创建服务端 `CurrentUser` 上下文，禁止 Controller 接受业务 `user_id`。
- 为用户表建立复合外键和可选 RLS；实现 A/B 用户越权测试模板。
- 管理员角色只做最小骨架，默认不能无审计浏览用户敏感数据。

### 涉及模块

Auth/User Controller 与 Service、Spring Security、Redis Session/Rate Limit、PostgreSQL 用户表、审计、前端登录/注册布局。

### 验收标准

- 注册、登录、退出、会话过期、账号锁定和 CSRF 测试通过。
- 数据库不含明文/可逆密码，日志不出现密码或 Session。
- A 用户无法读取、更新或删除 B 用户任何测试资源，统一返回 404。
- Session 注销立即生效；并发请求和连接池下 RLS 上下文不会串用户。
- CodeQL、依赖和秘密扫描无未处理高危项。

### 不做什么

- 不做微信登录、短信登录、社交账号矩阵或复杂组织/企业租户。
- 不开发完整客服/运营后台。
- 不允许“前端传 user_id 方便调试”的临时后门。

## 阶段 4：简历和求职目标

> 实施状态：第 4 轮已完成。已交付 PDF/DOCX/TXT 上传、ClamAV 安检、AES-256-GCM 加密、异步文本提取、删除清理、版本化求职目标、审计和 RLS 隔离。

### 目标

让用户安全维护一份当前简历和版本化求职目标，作为 AI 匹配的可信输入。

### 具体功能/工作

- 建立 `resumes`、`job_preferences` 表和用户内复合约束。
- 实现简历上传、格式/魔数/大小校验、恶意文件扫描、异步解析、当前版本和删除链路。
- 使用私有文件存储、随机对象 Key、静态加密和受控下载。
- 实现目标职位、城市、薪资、经验、学历、行业、公司偏好/排除、关键词等配置。
- 对当前本地版 PDF 提取、简历文本和平台筛选配置做字段级迁移，删除 Cookie/本地路径依赖。
- 增加隐私提示、数据删除状态和操作审计。

### 涉及模块

Resume/Preference API、文件存储和解析 Worker、PostgreSQL、Web 表单和上传 UI、审计与配额预留。

### 验收标准

- A 用户不能访问 B 用户文件，即使知道对象 Key 或简历 ID。
- 不支持格式、伪造 MIME、超大文件、路径穿越和恶意文件被拒绝。
- 每用户同一时刻只有一份当前简历和一份当前偏好，历史版本可追溯。
- 删除后文件立即不可访问，并能验证原文件、派生文本和缓存最终清理。
- 解析错误对用户可理解，日志不含简历全文。

### 不做什么

- 不做公开简历链接、简历市场、复杂在线编辑器或 AI 全自动改写。
- 不保存招聘平台内置简历账号数据。
- 不支持无限文件格式和超大视频/图片简历。

## 阶段 5：岗位池和 AI 匹配

> 提前完成的底座：第 4 轮已建立 `job_posts`、用户内去重索引、RLS、只读列表/详情 API 和 Web 页面。岗位写入、真实插件采集、`job_matches`、AI 队列和计量仍属于本阶段及阶段 7。

### 目标

建立统一用户岗位池和可追溯、可重试、可计量的 AI 匹配流程。

### 具体功能/工作

- 建立 `job_posts`、`job_matches`，统一 Boss/智联等平台岗位字段。
- 复用并增强 `SalaryParser`、岗位 DTO、公司/岗位去重和优先公司逻辑。
- 实现岗位列表、详情、筛选、分页和用户内去重。
- 实现单条/批量 AI 分析 API、Redis Streams 队列、Worker Consumer Group、重试和死信状态。
- 对简历做最小化脱敏，Prompt/模型版本化，严格解析评分、决策、优点、风险和建议招呼语。
- 记录模型、Token 用量、耗时和失败码；AI 结果不自动创建已确认任务。
- 此阶段岗位可以通过受控测试数据/API Fixture 导入，真实插件接入留到阶段 7。

### 涉及模块

Job/Match API、PostgreSQL、Redis、AI Worker、现有 `JobAiAnalysisService`/`AiService` 可复用逻辑、前端岗位池与分析 UI。

### 验收标准

- 重复岗位不会无限新增；同一输入指纹重复分析不会重复扣量或创建冲突结果。
- Worker 重复消费、崩溃重启和模型超时不会造成错误状态或无限重试。
- AI 输入经过脱敏，输出 Schema 校验失败进入 `FAILED`，不会默认建议投递。
- A 用户的 Worker 消息无法读取 B 用户简历/岗位。
- 列表性能和关键索引通过有代表性数据量测试。

### 不做什么

- 不做服务器端招聘平台爬虫或登录。
- 不追求第一版支持所有招聘平台和所有 AI 供应商。
- 不让 AI 直接确认、投递或自动修改用户简历。

## 阶段 6：投递清单和用户确认

### 目标

建立“建议不等于授权”的投递清单和严格状态机，保证只有用户明确确认的任务才可进入执行通道。

### 具体功能/工作

- 建立 `delivery_tasks`、`delivery_task_events` 和状态迁移规则。
- 支持从岗位/Match 创建待确认任务、列表筛选、编辑招呼语、确认、重新确认和跳过。
- 记录确认人、确认时间、确认版本、内容快照和审计事件。
- 实现创建/确认幂等、乐观锁、活动任务唯一约束和成功终态保护。
- 使用模拟插件/测试执行器验证 pending、租约、成功、失败、暂停流程。
- Web 确认界面显示平台、公司、岗位、链接、招呼语和风险提示。

### 涉及模块

Delivery API/Service/状态机、PostgreSQL、审计、前端待确认卡片/表格/确认对话框、集成测试执行器。

### 验收标准

- 未确认任务在任何情况下都不能被执行器读取或开始。
- 修改已确认招呼语后旧确认立即失效。
- 并发确认/跳过/开始通过版本和状态机只产生一个合法结果。
- 每次状态变化都有不可变事件；成功任务不会被重复失败回传覆盖。
- 批量确认若启用，有条数限制、逐项展示和明确二次确认。

### 不做什么

- 不做无确认自动投递、默认全选或后台静默确认。
- 不接真实招聘平台页面执行，先验证服务端状态机。
- 不做复杂投递策略编排和跨用户共享清单。

## 阶段 7：插件云端绑定和岗位上传

### 目标

把当前 localhost Chrome Bridge 改造成最小权限、可撤销、用户隔离的 Cloud 插件客户端，并打通安全岗位采集。

### 具体功能/工作

- 实现一次性绑定码、`plugin_devices`、`plugin_tokens` 哈希、Scope、过期和撤销。
- 改造插件 API Base、鉴权 Header、错误处理、Token 本地安全存储和版本检查。
- Manifest 用正式 Web/API 和明确招聘平台域名替换 localhost，仅申请必要权限，禁止 `<all_urls>`。
- 复用 Boss/智联 content script、选择器、采集和现有 Node 测试，增加字段白名单和请求大小限制。
- 实现单条/批量采集、用户内去重、部分成功、幂等、限流和额度预检查。
- Web 提供生成绑定码、设备列表、最近在线和撤销入口。
- 建立开发版/预发布版/生产版不同扩展 ID 和后端环境。

### 涉及模块

Plugin API、Redis 绑定码、设备/Token 表、Chrome Extension Manifest/background/content scripts、Web 设备页、插件自动化测试。

### 验收标准

- 绑定码过期、复用、爆破和跨用户使用均失败；Token 明文不入库/日志。
- 撤销设备后旧 Token 立即不能调用任何插件接口。
- 插件只上传结构化岗位白名单，不上传 Cookie、页面全文或浏览器缓存。
- Manifest 无 `<all_urls>`、无 `cookies` 等非必要权限，新增域名经过评审。
- A 用户插件上传的岗位只出现在 A 用户岗位池；重复上传幂等。

### 不做什么

- 不做 Firefox/Safari、多浏览器商店全矩阵。
- 不自动安装扩展，不请求硬件指纹或浏览器历史。
- 不在本阶段执行真实投递。

## 阶段 8：插件执行投递和结果回传

### 目标

完成“用户确认 -> 插件领取 -> 用户浏览器执行 -> 结果/暂停回传”的最小安全闭环。

### 具体功能/工作

- 实现插件 pending、start、success、fail、pause 接口和 Scope。
- 使用原子领取、指定设备、短租约、execution ID、版本和幂等键防止重复投递。
- 复用当前 Boss/智联投递脚本，但移除 localhost、Cookie API 和服务端 Playwright 依赖。
- 插件只执行当前用户 `CONFIRMED` 任务，展示即将执行的岗位和招呼语。
- 对验证码、登录失效、风控、页面变化、人工选择统一暂停，不自动重试。
- 回传受控错误码和最小证据，不上传 Cookie、截图、DOM 全文或敏感页面内容。
- 建立平台沙盒/测试账号可行时的 E2E；无沙盒时以页面 Fixture、录制契约和小范围人工验证为主。
- 加入插件版本强制升级、平台适配开关和紧急停用能力。

### 涉及模块

Delivery/Plugin API、任务租约和事件、Redis 通知、Chrome Extension background/content scripts、Web 实时状态和人工处理提示、E2E/契约测试。

### 验收标准

- 没有 `confirmed_at` 的任务无法出现在插件列表；插件不能自行确认。
- 两台设备并发领取同一任务，只有一台成功；租约过期可安全恢复且不会重复成功。
- success/fail/pause 重复回传幂等，成功终态不会倒退。
- 检测到验证码、登录失效或风控后立即停止并显示人工处理提示。
- 服务器网络层没有招聘平台登录/投递请求，数据库和日志没有平台 Cookie。
- 至少 Boss/智联中一个平台完成受控端到端验收后，再逐个平台开放。

### 不做什么

- 不自动绕过验证码、风控和平台频率限制。
- 不做服务器代投、账号池、代理池或无确认批量投递。
- 不在未验证的平台页面继续“猜按钮”执行。

## 阶段 9：额度、基础付费和后台管理

### 目标

先建立可靠额度计量和最小运营能力，再以功能开关方式接入简单付费；第一版不发展为复杂计费平台。

### 具体功能/工作

- 建立 `user_quotas`、`quota_usage_logs`，支持 AI 分析等资源的预占、确认、释放和幂等。
- 建立免费套餐、周期刷新、额度不足提示和用户用量页。
- 建立最小后台：用户状态、设备状态、队列健康、额度人工调整、失败任务统计；敏感字段默认隐藏。
- 所有管理员跨用户操作要求独立角色、操作理由、二次确认和审计。
- 预留 `subscriptions`、`orders`；MVP 可先用免费额度和人工开通。
- 如果业务验证后确需付费，只接一个支付渠道、一个/少量固定套餐，回调验签、幂等、对账和退款状态先测试后开放。
- 建立成本告警、额度异常、支付回调失败和人工对账流程。

### 涉及模块

Quota/Subscription/Order/Admin API、PostgreSQL、Redis 锁/限流、支付适配器（可选且默认关闭）、Web 用量页和后台、审计与监控。

### 验收标准

- 并发/重复 AI 请求不会重复扣额度；失败任务按规则释放预占。
- 用户只能看到自己的额度和订单；管理员调整有前后值、理由和审计。
- 套餐到期/额度不足行为可预测，不影响已有数据读取和用户导出/删除权利。
- 如启用支付：回调验签、重复回调、金额篡改、订单关闭、退款和对账测试全部通过。
- 后台默认不展示完整简历、Token、Cookie 或 AI Key。

### 不做什么

- 第一版不做微信登录、复杂会员等级、优惠券、分账、多币种、多渠道、自动税务或企业账单。
- 不因额度不足而绕过用户确认或安全暂停机制。
- 不把支付凭证、银行卡信息或完整回调敏感报文写入数据库。

## 跨阶段发布门槛

每个阶段 PR 合入 `main` 前至少满足：

- 功能、权限、失败路径和状态机自动化测试通过。
- A/B 用户隔离测试覆盖新增表、接口、队列和文件。
- 没有 Cookie、Token、API Key、`.env`、真实简历和账号数据进入 Git/日志/Artifact。
- 文档、迁移、回滚和监控同时更新。
- CI、CodeQL、依赖/镜像/秘密扫描没有未处理高危项。
- 涉及插件权限、数据库结构、支付或数据删除时完成专项评审。
- 不自动合并 PR，不删除分支，不 force push。

## 本地版模块迁移顺序

| 当前能力 | 建议进入阶段 | 策略 |
| --- | --- | --- |
| CI、CodeQL、Docker/Release 框架 | 1-2 | 复用框架，替换 Cloud 构建和部署目标 |
| Next.js UI 基础、分析表格/筛选/图表 | 3-6 | 复用组件，重写鉴权、API 和路由 |
| `SalaryParser`、岗位 DTO/去重 | 5 | 优先抽成无状态领域模块并补 PostgreSQL 测试 |
| AI Prompt/结果解析 | 5 | 移入 Worker，增加脱敏、版本、幂等和额度 |
| 待确认状态语义和确认弹窗 | 6 | 改为显式英文状态机与不可变事件 |
| Boss/智联插件采集脚本 | 7 | 保留平台适配，重写 Cloud 鉴权和字段白名单 |
| Boss/智联插件投递脚本 | 8 | 只执行已确认任务，加入租约和安全暂停 |
| SQLite Mapper/Flyway | 不直接迁移 | 按 PostgreSQL 和 `user_id` 重新设计 |
| Cookie 保存、服务端 Playwright 投递 | 不进入 Cloud | 仅留本地版，Cloud 不提供等价服务器能力 |

## 第一版完成定义

第一版 SaaS 可以称为“可受控试用”，至少需要阶段 0-8 全部通过，并具备阶段 9 的免费额度和最小后台；真实支付可以在需求验证后单独启用。完成定义不是“页面能打开”，而是多用户隔离、隐私删除、插件撤销、用户确认、任务幂等、验证码暂停、监控备份和安全测试均可验证。
