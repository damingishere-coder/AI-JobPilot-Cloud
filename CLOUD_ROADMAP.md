# AI-JobPilot-Cloud SaaS 开发路线

本文描述“投递牛马 SaaS 云端版”的建设顺序和阶段完成标准。云端负责资料、分析和任务编排；招聘平台页面上的采集与投递始终由用户浏览器插件完成。

## 总体原则

- 多用户数据必须严格隔离，所有个人资料和任务记录都按用户授权访问。
- 简历属于高敏感个人资料，应具备受控访问、加密存储和用户主动删除能力。
- 服务器不接收或保存招聘平台账号、密码、Cookie、登录令牌及浏览器会话。
- 插件只能在用户已登录的浏览器页面中工作，不绕过验证码、风控和平台限制。
- 每次投递都必须有清晰的用户确认记录、执行结果和失败原因。

## 阶段 1：用户与求职资料

### 用户系统

- 建立注册、登录、退出和会话管理能力。
- 密码只保存安全哈希，接口和数据查询默认按当前用户隔离。

完成标准：两个不同用户无法访问彼此的资料、配置、岗位和任务；登录、退出和会话失效流程可测试。

### 简历上传

- 支持用户上传、查看、替换和删除自己的简历。
- 提取后续 AI 匹配所需的结构化信息，同时保留原文件的访问控制和删除链路。

完成标准：上传格式和大小受限制；文件不可被未授权用户访问；用户删除后原文件和派生内容进入可验证的清理流程。

### 求职目标配置

- 支持目标职位、城市、薪资、行业、公司偏好和排除条件等配置。
- 支持用户维护当前生效的求职目标，并供岗位筛选和 AI 匹配统一使用。

完成标准：配置可以保存、修改和停用，岗位筛选与匹配使用同一份生效配置。

## 阶段 2：岗位与 AI 决策

### 岗位池

- 接收插件采集或其他合规来源提供的结构化岗位。
- 对岗位去重、标记来源、记录更新时间和失效状态。

完成标准：重复岗位不会无限新增；用户可以按来源、城市、状态和时间筛选岗位。

### AI 匹配

- 使用用户授权的简历和求职目标分析岗位匹配度。
- 保存评分、理由、风险点和模型版本，允许失败重试与结果追踪。

完成标准：同一次分析可追溯输入范围、模型版本和结果；AI 结果只提供建议，不自动触发投递。

### 投递清单

- 用户可将岗位加入待确认、已确认、执行中、成功或失败等状态的清单。
- 支持单条和批量确认，但确认动作必须明确、可审计。

完成标准：只有已确认任务能交给插件执行；重复执行、状态冲突和失败重试有明确规则。

## 阶段 3：浏览器插件闭环

### 插件绑定

- 使用短期、一次性的绑定流程把浏览器插件关联到当前云端用户。
- 绑定凭证可撤销、可过期，不包含招聘平台登录信息。

完成标准：用户可查看并解绑自己的插件；过期、撤销或属于其他用户的绑定无法访问任务。

### 插件采集

- 插件在用户已登录并主动打开的招聘平台页面中采集岗位信息。
- 仅向云端上传岗位所需的结构化字段，不上传整份浏览器配置、Cookie 或无关页面数据。

完成标准：采集数据可标记平台和来源页面；权限、字段范围和失败提示对用户透明。

### 插件执行投递

- 插件只领取当前用户已经确认的投递任务，并在用户浏览器中执行。
- 插件回传成功、失败、需要人工处理等结果，不向服务器回传登录会话。

完成标准：未确认任务不能执行；执行前后都有用户可见状态；验证码、风控或页面异常会停止并提示用户处理。

## 阶段 4：额度和付费系统

- 建立免费额度、套餐、用量计量、订单、支付结果和退款状态。
- AI 分析、岗位处理或其他计费动作采用可追溯、可防重复扣费的额度流水。
- 支付回调必须验签并支持幂等处理，后台可审计额度变更原因。

完成标准：并发请求不会重复扣费；支付结果可对账；退款、套餐过期和额度不足均有明确状态与用户提示。

## 发布门槛

每个阶段进入生产环境前，至少需要通过：

- 权限隔离、输入校验和敏感信息泄露测试。
- 关键接口、失败重试和状态流转测试。
- 数据备份、删除、审计和异常恢复演练。
- 浏览器插件权限检查，以及用户确认投递的端到端验证。
- 额度与支付阶段的回调验签、幂等和对账测试。
