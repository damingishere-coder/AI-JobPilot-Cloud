# AI-JobPilot-Cloud P0～P9 验收报告

验收日期：2026-08-24

验收范围：`AI-JobPilot-Cloud_SaaS_P0-P10_DevPlan.md` 的 P0～P9，以及用户指定的十项重点安全检查。

数据边界：只使用源码、自动化测试、合成数据和腾讯云预发布服务器；未接真实支付、未导入真实用户、未上传真实简历、未启用无确认自动投递。

## 总结

- P0～P9 的业务主链路已经具备；本轮发现的明确日志泄露缺口已修复并补测试。
- P6 的 `profile_id` 采用显式验收决策：当前 `user_profiles.user_id` 是一对一主键，任务用 `user_id` 绑定档案；当前新建接口始终写入 `job_match_id`，固定实际 `resume_id` 与 `preference_id`。数据库仍允许历史兼容空值。本轮未擅自开发多档案新功能，而是把单档案产品范围和现行八状态状态机写回计划与设计文档。
- 腾讯云预发布服务器已经运行 Compose 全栈，PostgreSQL、Redis、ClamAV、Web、API、Worker、Nginx 健康；数据库和 Redis 未暴露公网，应用数据库角色已做 RLS 权限验证。
- 可以继续完成 P10，但目前仍不能邀请真实用户。阻塞项是正式域名/备案/HTTPS、异地加密备份、账号级删除能力或合规确认、合成数据插件全链路验收。

## 本轮验证证据

- `./gradlew.bat test`：退出码 0，`BUILD SUCCESSFUL`。
- `./gradlew.bat build`：退出码 0，`BUILD SUCCESSFUL`。
- `cd front && pnpm lint`：退出码 0，0 error、36 个存量 warning。
- `cd front && pnpm build`：退出码 0，Next.js 16.3.2 生产构建成功，25/25 静态页面生成。
- `node --test chrome-extension/tests/*.test.cjs`：退出码 0，123/123 通过。
- `docker compose config --quiet`：退出码 0；加入日志轮转配置后再次验证通过。
- `MatchWorkerTest` 日志防泄露目标测试在最终测试数据调整后再次执行：退出码 0。

以上是本地验证结果；新 PR 的 GitHub CI 必须再次全绿，且 PR 合并部署后还要在服务器复核，才能证明这些修复已进入预发布环境。

## P0：新仓库初始化与 SaaS 定位

- 已完成项：Cloud 入口固定为 `CloudApplication`；README 说明本地版与 SaaS 边界；环境、日志、数据库、备份、简历和凭证类文件已加入忽略规则。
- 未完成项：无 P0 代码缺口。
- 风险项：旧本地版 Cookie/Playwright 源码仍与 Cloud 代码共仓，生产必须只启动 Cloud Boot Jar，并保持 Nginx 对旧本地接口的 404 阻断。
- 需要补修的文件：无；部署时复核 `Dockerfile`、`docker-compose.yml` 和 Nginx 模板。
- 是否影响继续做 P10：不影响；误启动旧入口会阻止 P10 最终通过。

## P1：SaaS 架构与设计

- 已完成项：架构、数据库、API、安全和部署边界文档齐全；用户身份从服务端 Session/插件 Token 推导，不信任客户端 `user_id`。
- 未完成项：本轮开始时数据库/API 文档落后于现行迁移和状态机，现已对齐。
- 风险项：以后每次数据库迁移都需要同步设计文档，避免运维按旧字段或旧状态处理。
- 需要补修的文件：已补修 `CLOUD_DATABASE_DESIGN.md`、`CLOUD_API_DESIGN.md`、开发计划中的单档案说明。
- 是否影响继续做 P10：补修后不影响。

## P2：腾讯云基础设施

- 已完成项：OpenCloudOS 服务器已安装 Docker、Compose、Git、Nginx；已增加 2 GiB Swap；Compose 全栈已启动并通过健康检查；5432、6379、6866、8080、8888、8889 未向公网开放；SSH 使用密钥，22 仅允许当前管理 IP。
- 未完成项：没有正式域名、ICP备案、DNS 和 HTTPS；宿主机外层 Nginx 尚未启用正式站点；备份仍只在服务器本地磁盘。
- 风险项：没有 HTTPS 时 `AUTH_COOKIE_SECURE=true` 的正式登录无法使用；同盘备份不能抵御主机或磁盘损坏。
- 需要补修的文件：部署文档已补 OpenCloudOS 流程；剩余是腾讯云控制台、域名、证书和异地备份配置，不是新业务代码。
- 是否影响继续做 P10：可以继续 P10；上述未完成项阻止 P10 最终通过和真实用户准入。

## P3：用户系统与多用户隔离

- 已完成项：密码使用 Argon2 哈希；登录限流和锁定；Web Session、CSRF、HttpOnly/SameSite Cookie；PostgreSQL RLS、服务层 `user_id` 过滤和 A/B 用户隔离集成测试；普通用户访问后台返回 403。
- 未完成项：无已知代码缺口。
- 风险项：每套生产数据库都必须确认运行角色不是表 owner、没有 `BYPASSRLS`；腾讯云预发布库已完成一次权限验证。
- 需要补修的文件：无。
- 是否影响继续做 P10：不影响。

## P4：简历与求职目标

- 已完成项：上传大小/类型校验、ClamAV、私有加密存储、解析状态、版本化求职目标、用户范围读取与删除；日志不记录简历全文。
- 未完成项：账号级自助彻底删除尚未实现；这属于真实用户上线阻塞项，不在本轮新增业务范围内。
- 风险项：上线前仍需用合成文件演练上传、加密、备份、恢复和清除，不能使用真实简历。
- 需要补修的文件：现有删除设计记录于 `CLOUD_PRIVACY_CHECKLIST.md`，后续需单独实现并验收账号删除。
- 是否影响继续做 P10：不影响继续准备；影响真实用户准入。

## P5：岗位池与 AI 匹配

- 已完成项：岗位白名单采集、用户级去重、按用户读取、匹配任务幂等、PII 清理、结构化 AI 结果与人工决策；AI Key 只由 Worker 的 Secret/环境配置读取。
- 未完成项：腾讯云没有配置真实 AI Key，也未做付费真实调用，符合本次禁止真实调用的边界。
- 风险项：正式启用第三方 AI 前需要完成数据处理披露、额度和费用告警，并用合成内容验证供应商链路。
- 需要补修的文件：本轮已从数据库设计中删除不存在的 `source_device_id`、`raw_payload` 描述。
- 是否影响继续做 P10：不影响；影响真实 AI 功能开放。

## P5.5：云端数据安全

- 已完成项：Secret/configtree 注入、审计白名单与脱敏、CORS 精确来源、限流、非 root 容器、内部网络、Cloud 应用 stdout/stderr 日志；本分支为全部 Compose 服务补充了有限大小的 Docker `json-file` 日志轮转。
- 未完成项：本轮发现 `MatchWorker` 会记录 `exception.getMessage()`，现已改为只记录异常类型，并新增“异常消息不得进入日志”测试。
- 风险项：接入新的第三方 SDK 或 APM 后仍需复核其默认日志内容。
- 需要补修的文件：已补修 `src/main/java/com/getjobs/cloud/match/MatchWorker.java` 和对应测试。
- 是否影响继续做 P10：修复并通过测试后不影响。

## P6：投递清单与用户确认

- 已完成项：从已完成 `job_match` 创建任务、同一匹配幂等、用户确认/跳过/修改招呼语、乐观锁、不可变事件、确认时额度幂等扣减；任务必须经过用户明确确认。
- 未完成项：无已知代码缺口。
- 风险项：当前是单基础档案模型，数据库为早期结构保留可空 `job_match_id`，只有当前新建接口保证写入匹配；未来若支持一个用户多份独立档案，必须新增独立 `profile_id`，不能沿用当前假设。
- 需要补修的文件：已补修开发计划、`CLOUD_DATABASE_DESIGN.md`、`CLOUD_API_DESIGN.md`，说明单档案映射和现行状态机。
- 是否影响继续做 P10：不影响。

## P7：插件绑定与岗位上传

- 已完成项：一次性绑定码；插件 Token 明文只返回一次、数据库只存 SHA-256 哈希；设备可撤销；岗位上传字段白名单明确拒绝 Cookie、Authorization、密码、LocalStorage、SessionStorage 和完整页面。
- 未完成项：正式域名尚未确定，因此生产插件绑定地址尚未最终配置。
- 风险项：插件 Token 必须保存在用户浏览器本地才能调用 Cloud API；浏览器账户或本机失陷时存在客户端 Token 风险。Manifest 目前仍声明广泛的可选 `https://*/*`，运行时虽只请求用户填写的精确 HTTPS Origin，仍属于最小权限和插件商店审查风险。
- 需要补修的文件：正式域名确定后评估把 `chrome-extension/manifest.json` 的远程可选权限收紧为正式 Cloud 域名，并同步测试和商店权限说明；拒绝精确 Origin 授权时必须继续保持不保存 Token。
- 是否影响继续做 P10：不影响；正式域名和商店审查影响真实发布。

## P8：插件拉任务与结果回传

- 已完成项：只领取当前 Token 用户的 `CONFIRMED` 任务；领取/start/success/fail/pause 均校验用户、设备、Scope、版本、租约和幂等键；验证码、登录失效、风控只能暂停并要求人工处理；没有绕过验证码、自动登录或 Cookie 上传路径。
- 未完成项：`DELIVERY_EXECUTION_ENABLED` 仍保持关闭；尚未在正式域名上完成合成任务的浏览器端闭环。
- 风险项：在闭环测试完成前打开执行开关可能造成不可控平台操作。
- 需要补修的文件：无代码补修；在 P10 清单中保留合成数据 E2E（端到端）验收门槛。
- 是否影响继续做 P10：不影响继续准备；阻止真实执行开关开启。

## P9：额度系统与基础后台

- 已完成项：额度余额、预留/确认/释放/消费使用事务、行锁和 `(user_id, operation_key)` 唯一约束；管理员接口要求 ACTIVE ADMIN；普通用户 403；后台输出不含密码、Token、API Key、Cookie 或简历全文。
- 未完成项：没有真实支付、订阅或正式管理员运营流程，符合本次禁止接真实支付的要求。
- 风险项：首次生产管理员授予必须走受控人工流程并留审计，不得通过环境变量自动提权。
- 需要补修的文件：无代码补修；P10 需用合成账号验证额度重放和后台只读统计。
- 是否影响继续做 P10：不影响；影响正式运营开放。

## 十项重点安全结论

1. A 用户访问 B 用户数据：未发现可行路径；服务层用户过滤、复合外键、RLS 和集成测试共同限制；生产仍需持续确认应用角色 `NOBYPASSRLS`。
2. 密码存储：Argon2 哈希，不保存明文。
3. 插件 Token：数据库只保存 SHA-256 哈希；浏览器本地保存客户端 Token 是明确的终端边界风险。
4. Secret 来源：项目不使用 JWT；数据库密码、Redis 密码、密码 Pepper、数据加密 Key、AI Key 均由环境变量指向的 Docker Secret/configtree 提供。
5. 日志：本轮修复 Cloud `MatchWorker` 最后一处已发现的 `exception.getMessage()`；Cloud 生产包静态扫描和预发布最近日志抽样未发现 Token、密码、简历全文或 API Key 值。旧本地入口不属于 Cloud 生产包，仍不得误启动或暴露。
6. 后台权限：普通用户不能访问 `/api/admin/**`，有 403 集成测试。
7. 额度扣减：事务、行锁、唯一 operation key 保证幂等。
8. 插件敏感数据：服务端白名单和扩展契约均禁止上传 Cookie、招聘平台账号密码及浏览器存储。
9. 数据库和 Redis：Compose 不发布其端口；腾讯云安全组与主机监听复核未发现公网暴露。
10. Git 仓库卫生：已知敏感路径（`.env`、`.secrets/`、数据库、日志、备份、真实上传目录、`cookie.json` 等）已忽略；不能依赖文件名覆盖所有 Cookie/Token/API Key，提交前还必须对 staged diff 做内容模式扫描。

## 进入 P10 的结论

P0～P9 代码与设计缺口在本分支补齐后，可以继续 P10 上线准备；但“完成 P10”与“可邀请真实用户”仍是两个不同结论。只有正式域名与 HTTPS、异地加密备份、账号删除/合规方案、合成插件闭环和最终部署版本回归全部通过，才可考虑小范围真实用户测试。
